# 分发脚本与最终成果报告

## 范围

- 以 `codex/context-reduction` 最终运行规范为准制作逐仓同步脚本；不在真实项目执行分发、不推送、不部署。
- 回看本项目可访问的全部主对话（含归档），以最终代码和规则核对结论；报告不呈现中间往复，不把已撤销方案写成成果。
- 不新增 A 类流程改造；分发需要的项目专属约束/状态/配置判断必须保留，不以模板覆盖代替迁移。

## 交付顺序

1. 查对话、最终规范及现有模板/检查器，确认分发与报告边界。
2. 实现单仓、固定方法版本、独立 worktree、可重复运行的同步脚本；未知自定义文件不覆盖，语义迁移留明确接续清单。
3. 真实临时 Git 仓验证：新旧模板、项目自定义、脏主树、版本漂移、恢复、失败回收、检查与安全退出。
4. 形成最终版报告和使用说明，核对整体维护量口径；本地提交交付。

进度：实现与验证完成，本批本地提交收尾。报告以方法内容基线 `2e9ed33` 为准；本批交付分发工具、测试与文档，并将两句残留计时措辞对齐为已确认的可选规则，不改变 A 类流程。

## 交付及核对

- 分发入口：`scripts/sync-method.cjs`，默认 check 只读；prepare / verify / finish / 可选 integrate。用户已明确由各仓 Codex 接着完成约束迁移，不止复制文件。
- 使用说明：`guide/07-同步方法论.md`；最终报告：`reports/方法论最终成果报告-2026-09-08.md`。
- 回看本机可访问的 8 个方法论仓主对话（含归档）及 2 个项目列表关联 awuchi 对话，逐页核到最早记录；用最终执行文件与 Git 补核更早改造。不导出原始聊天或秘密，不写中途往复。
- 核对 OpenAI Docs 的 AGENTS.override 遮蔽、项目 TOML 角色和模型继承；不修改用户全局配置，不固定型号。
- 方法内容 14 组回归全绿；新增分发真实临时 Git 回归涵盖固定来源、脏原树隔离、已知/未知模板、自定义候选、恢复不覆盖、源码漂移、活跃引用、hook、范围外文件、提交后回收失败接续、可选快进与幂等。
- 分发测试不读取真实凭据或访问业务环境；凭据检查在该夹具里用明确的无外部访问替身，不宣称测试了真实凭据扫描效果。
- 六个真实项目只读预检：每仓均有 3 个待迁移 Standards；4 仓有在制品。未创建真实项目 worktree，未修改原入口/config/hook/状态，未推送/部署。
- 文档本地链接、Node 语法、check-paths（109 文件）及 diff whitespace 检查通过。
- 统计按最终 diff，不累加中间提交：方法内容相对 7c28db4 为 +6,183/−5,293（净 +890），正文/模板/配置净 −2,978；本批交付工具/测试/报告另列，不算已验证节时。
