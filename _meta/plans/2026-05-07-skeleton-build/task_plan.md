# 任务计划 · hact-method 骨架搭建

## 阶段目标
在 `skeleton/` 下完成 hact-method 方法论骨架，形成可作为第二阶段（结构层规范）输入的成品文档集。

## 完成标志
1. 用户身份模型完稿（用户 vs 角色 vs 任务 三者关系、写权限边界、跨工作区身份打通）
2. 角色清单文档完稿（5 角色定义、与 v1 角色的映射、"角色 = 任务工具书"如何落到形态）
3. 工作区定义文档完稿（项目根 / dispatch / group-code 父级 三个工作区的边界、心态、文件读写权限）
4. 任务全谱完稿（所有 task.type 列举、type 编码、归属 Gate、加载规范、产物）
5. 状态机文档完稿（任务状态流转、双向暂停、拉取/释放/召回机制）
6. Gate 与子阶段定义完稿（5 个 Gate 内涵、A/B 类差异、Gate 子状态聚合规则）
7. 骨架自检通过（覆盖度、互引一致性）

## 任务列表

| # | 任务 | 状态 |
|---|------|------|
| 0 | 列骨架文档清单（每份 = 一个文件名 + 一句话目的 + 大纲），跟用户对一遍 | ⏸️ 下一步 |
| 1 | 用户身份模型 | ⏸️ |
| 2 | 角色清单 | ⏸️ |
| 3 | 工作区定义 | ⏸️ |
| 4 | 任务全谱（type 编码 + 归属 Gate + 产物） | ⏸️ |
| 5 | 状态机 | ⏸️ |
| 6 | Gate 与子阶段定义 | ⏸️ |
| 7 | 骨架自检（覆盖度 + 互引一致性） | ⏸️ |

写作顺序考量：先底层概念（用户身份）→ 再容器（角色、工作区）→ 再流程（任务全谱、状态机、Gate）。具体顺序以任务 0 列清单时再敲定。

## 关键决策（继承自 BRIEF.md，不要绕回）

见 `BRIEF.md` "关键设计决策" 段（16 条）。本 plan 在写作过程中如对其中任何一条产生疑问，先在 progress.md 记下，不擅自推翻。

## 下一步起点

任务 0：列骨架文档清单。
- 每份文档定一个文件名（在 `skeleton/` 下）
- 每份给一个一句话目的 + 三五行大纲
- 列完后跟用户对一遍再开写

## 仍 park 的项

来自旧仓重构讨论的小细节，落地阶段补即可，无阻断：
- a 迭代废弃流程
- b B 类任务包修订
- d 机制层兜底（Stop hook）
- e method-pending 并发
- f deployment.config 时机
