# progress — Gate 签署前冷核完成判据

## 2026-06-18 · 会话 1（起专题）

- 起因：用户问"同事开发时方法论没同步到最新，什么情况下会发生"。初分析聚焦**同步机制**（Step 0 静默失败、长会话不重跑、相对路径假设等）。
- 转折：用户出示另一会话回复——V4 PRD 缺 `入口`+`draft-ux` 字段，经核实是**已生效规范没执行**（`6b39b49` 2026-06-03 就进了判据，06-16~06-18 没碰该 spec），不是同步滞后。
- 重新定位：症状底下两类根因（真/假同步滞后），本专题**只打第 2 类**——Gate 签署前冷核完成判据。
- 已核实证据见 findings.md F1（git log 空结果 + 判据第 59–60 行）。
- 设计方向定：复用 2026-06-18 draft-ux「Step 3.5 派全新 subagent 冷审」范式，推广到 Gate 完成判据完整性核对。
- 已建专题目录 + `.current_plan` 指针切到本专题；写入 task_plan/findings/progress。

**下一步**：阶段 1 现状盘点——列 5 个 Gate 各 task 完成判据（标机械可查/需判断）+ 摸清既有相邻机制（plan-sprint Step 3.5 独审 / pr-review / adversarial-review / draft-ux Step 3.5）边界。等用户对开放问题（尤其 Q1 全挂 vs 择重、Q2 与既有 Step 3.5 合并、Q5 是否同期改 Step 0）给方向后再推进。

## 2026-06-18 · 会话 1（续：方案定稿）

- 推演链：memory vs 最新文件 → 三层真相（F6）→ 重读不够 → 防患于未然（模板空槽+linter）→ 否掉"CLAUDE.md 加一句比方法新旧"（治错根因）→ 冷核+status.yml（纠正：status.yml 是账本不放动作，且用户决定不碰）→ hook 闸门 → 六种伪造 pass 情形 + 产出方=受益方根因 + 人工抽看兜底。
- 产出 `design.md`：完整修改方案（原因/解决方案/遗留漏洞/人类弥补动作/开放决策）。**未动任何 spec，待用户确认。**
- 待用户拍的关键决策：开放问题 3（软步骤先行 vs 现在就上 hook）最需拍；其余建议见 design.md §五。

## 2026-06-18 · 会话 1（续：本轮范围拍板）

- 用户决策：**本轮只做 Layer B 软版**——冷核步骤 + 凭证文件 `gate-checks/G{N}.md` + 完成判据补「已冷核」+ 人工抽看凭证；**hook 不加、Layer A 延后**。
- 已同步 design.md §五/§六、task_plan 阶段与延后清单。
- 明确取舍：软版无机械防线，"彻底不做"和"假装做"都靠人在 Gate 现场兜，待后续上 hook 补硬闸。
- **下一步（待用户放行后动手）**：阶段 1 改 spec——5 个 Gate 对应 task 的 specs-execution 插冷核步骤 + 凭证约定 + 人工抽看；specs-structural 补判据。

## 2026-06-18 · 会话 1（续：落地完成）

- 修 draft-ux 回退：`9074062`（从 5d53088 恢复 Step2.5/3.5/红线/读清单/状态表）。
- Gate 冷核 B 软版落地：skeleton §7 协议 + 5 exec 短步骤 + 5 structural 判据补条 + STATUS 里程碑。
- 凭证路径定为可见的 `iterations/vN/gate-checks/G{N}.md`（软版命脉是人方便抽看），plan 文档同步去 dot。
- 机械自检通过：exec 5/5、structural 5/5、skeleton §7 在、无 `.gate-checks` 残留。
- 待办：提交 Gate 冷核改动（不推送，master 需人类确认）。

## 错误记录

| 错误 | 尝试 | 解决 |
|------|------|------|
| （暂无） | | |
