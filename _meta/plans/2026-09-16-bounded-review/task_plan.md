# 独立审查范围与收敛调整

## 011/012 反馈后的落实（方法论仓限定）

用户澄清本轮只改 hact，file-extract 只读参考；不分发、不修改项目 hook，不开展 heartbeat 实验。保留无关 `_meta/plans/2026-09-16-develop-continuation-pilot/` 在制品。

- [x] 原 preflight 核实际控制位置和下游职责，只写异常，不增映射表。
- [x] 静态规则沿真实消费方式选正反例，复用 fixture 和现有报告，不增“三件证据”表。
- [x] 同一 review-chain 入口增加中间轮校验：合法 revise/evidence-needed 可通过结构检查，pending/坏 SHA/断链不可通过；最终闭合不放松。
- [x] hook 从暂存差异定位本次审计；全局规划/依赖/共享资产检查保留，未变化的历史 round 不重审，不新增豁免账。
- [x] 末轮 round 即最终结论，停止要求单独 final-review.md，历史保留。
- [x] 真实 Git 与 shell hook 正反例、审查链/状态门/接续回归及范围检查完成。

代码调整复用已有 check-sprint、check-gate 和 hook；不新建检查器。完整迭代审计命令仍保留用于主动调查，不自动阻断每次无关提交。仅本仓落实，项目实际效果须以后另行采用。

完成状态：本地修改和验证已完成，用户已授权本次提交推送到 codex/context-reduction；7 组相关测试、路径检查及 diff 检查通过。未另派独审，未操作项目仓；结果见 validation.md 的本轮段。

## 007 反馈后的减法修订（用户已确认方案）

本轮仅在方法论仓修订，用户随后授权提交推送到 codex/context-reduction，不分发项目；不新增写集字段、轮次账或审查阶段。沿用下方第一版历史记录，不倒改其完成事实。

- [x] 复用 method-sync.json 固定来源，启动核版本、按同一 SHA 读取规范；hook 保持项目检查器和自有检查。
- [x] files 保留为当前落点，取消本任务包自登记；普通落点维护不单独 spec-round，共享冲突/业务契约判断保留。
- [x] 扩展现有 anchor 工具生成首审/复审/必要规格复核草稿；只生成机械事实与待核问题，不生成通过或关闭结论。
- [x] 报告编号和历史计数保留；三次代码审查额度从 Git 增量推导，同快照同问题集中补证一次，不无限补查。
- [x] 定向复审核完整相关调用链；以 007 形态的临时 Git 正反例检验，核回归与操作减少。

本轮本地修订与针对性验证完成，用户已授权本次提交推送，不分发项目。复用两个现有工具，无新增必填业务字段、计数账或审查阶段。新规正式采用需在项目完成既有同步，不能只读取最新 brief 而继续使用旧脚本。版本检查在启动/升级时一次；额度与停止条件由草稿生成器从 Git 推导、审查执行规范执行，历史审计检查不追溯重判旧任务的轮次许可。

用户已授权制定计划并逐项落实，随后授权提交推送到当前方法论分支 `codex/context-reduction`。仅调整方法论仓，不修改项目代码、历史审查报告、Gate 或 hook，不分发项目仓。

## 第一版决策（历史；本轮变更以上方减法修订为准）

- 保留 PRD、UX、TRD、规划、开发和联调各自职责；开发首审三个核心问题，专项由审查员按真实改动独立触发。
- 同一独立审查员默认接续；不参与实现；复审只核问题、增量和影响，确凿新问题可报，扩审须有失效依据。
- 证据须匹配实现、依赖/配置、执行路径和环境；允许独立核验执行者的原始日志，不采信自评代替证据。
- 同快照补证仍使用 round 报告，仍计 code_rounds、仍受三轮上限，不新增阶段或旁路。
- 新报告声明 review_policy: bounded-v1；检查器从该轮起核结论一致性，旧报告原样保留并按原规则核验，不追溯重写。声明后不得退回旧策略。
- 纯补证须 targeted、显式 evidence_only、相同 Git tree 和有效证据文件；实现或测试变化必须普通增量复审。
- 机械字段工具生成；不新增 profile、全维度覆盖表或工时填报。

## 执行清单

- [x] 1. 同步范围规则、开发/地基 brief、阶段分工和项目入口。
- [x] 2. 同步执行与结构契约：送审完成度、审查员接续、证据补充与上限。
- [x] 3. 实现报告兼容校验、纯补证与结论一致性；提供机械锚生成入口。
- [x] 4. 用真实临时 Git 正反例验证：空增量补证、伪补证、证据缺失、建议阻断、真实阻断、旧链兼容。
- [x] 5. 检查相关审查链/工作树/wave 回归与路径、一致性；记录结果及实际节时未验证的边界。

## 验收

不减少本包必要验证，不免除独立审查，不因 sensitive 全维度展开。无阻断且无待处理扩审时通过；待证据须有具体阻断证据缺口。修改后测试及配置均绑定实际快照，补证不清零轮次。只完成本仓落地，项目实际采用和节时留待后续验证。

## 完成记录

2026-09-16 本仓修改与验证完成，用户随后授权本次提交推送；结果见 `validation.md`。一致性核对另发现并修正敏感角色“全部边界核验”的旧措辞，以及结构层“各类均有测试”的旧要求。末轮为空增量时，工作树白名单现在核完整报告链，并以 preflight→final head 的净改动恢复 accepted 实现，防止漏验或误拦。

项目采用边界：没有写 file-extract 或分发副本；但项目 live 引用本仓规范、brief 和当前版检查器时，下次读取即可消费本次变化。已有代理上下文不会自动刷新。新格式须配套新检查器；不重审已完成任务，不重写历史报告。
