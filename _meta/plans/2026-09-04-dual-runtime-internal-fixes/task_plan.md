# 双运行时内部漏洞修复

> 日期：2026-09-04
> 分支：`codex/dual-runtime-internal-fixes`
> 输入：独立审查结论 `block`（1×S0、7×S1、4×S2）
> 边界：只修方法论内部闭合与自动证据；不修改项目仓，不把真实外部运行时试点伪装成本轮验收。

## 修复批次

| 批次 | 内容 | 状态 |
|---|---|---|
| A · 控制平面阻断 | develop 专属词泄漏；G1→G3 启动路由；TRD 独审冲突 | ✅ |
| B · 执行与契约边界 | 批次 task diff；asset-writes 就绪；B 类契约；V0 ledger 闭合 | ✅ |
| C · 证据真实性 | Git tree/diff/finding 审计；handoff 独立夹具与变异；hook 真执行；浏览器证据契约 | ✅ |
| D · 初始化与兼容 | 双运行时初始化清单；指南中立化；存量只读诊断边界 | ✅ |
| E · 全量验证与独立复审 | 全套测试、反向夹具、路径/中立检查、五轮独立复审 | ✅ |

## 完成结论

全面独审从首轮 `block`（1×S0、7×S1、4×S2）开始，经五轮“修复→独立复核”收敛为 `pass`。当前仓内可证明的启动、路由、独立审查、固定 diff、批次白名单、依赖就绪、B 类契约、V0 ledger、联调证据、hook、双运行时项目诊断和 handoff 静态契约均已闭合。真实外部运行时试点仍是下一阶段，不属于本计划伪造的完成条件。

## 不变量

1. V0 仍由人类决定走或跳。
2. Gate、首次独立完整审查、固定 diff 与最终全量验证不能因修复降级。
3. 运行时专属命令、模型和代理配置只住 `templates/runtime/{cc,codex}.md`。
4. 自动检查必须对真实产物或真实 Git 对象工作，不用同源硬编码证明自己。
5. 存量项目未知键保持兼容；任何分发仍需逐仓授权。
