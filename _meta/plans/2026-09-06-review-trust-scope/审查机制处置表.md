# 审查机制处置表

依据：[全局重审工作约定](全局重审工作约定.md)。前两批已实施记录减法与接缝职责合并；第三批仅评估粒度，结论为暂不合并审查时点，不新增运行义务。

目标流程：实现者按已确认契约完成改动并自测 → 自动验证提供可复现结果 → 独立审查核契约、实际改动与证据盲点 → 按根因整改并定向复核 → 整合验证组合行为 → 用户裁决业务取舍和需接受的风险。

最少输入为权威契约、适用规范、固定改动版本、验证证据；复审加前次独立报告。问题正文只存逐轮报告；状态文件保留结论、报告索引和现有成本汇总。preflight 与恢复记录暂留其既有用途，不新增状态或协议版本。

| 质量目标 / 机制 | 当前承接环节 | 收益证据、重复或成本 | 去向与保障代价 | 本批状态 |
|---|---|---|---|---|
| 需求一致、承诺可验 | G1 前整份 PRD 独审 | 查需求矛盾/不可验/跨功能缺口；延后会让原型和设计先按错误承诺生产。暂无单独命中率对照 | 保留整份首审；用户仍决定业务取舍 | 三批：不并入 TRD 审查 |
| 技术设计承接需求 | G2 前整份 TRD 独审 | 查字段/接口/数据承接；07-30 记录有冲突载体不可机读的设计阻断。延后会让包按错设计生成 | 保留设计审查；不把任务包忠实抄错设计算覆盖 | 三批：不并入任务包审查 |
| 已声明交互路径存在 | 原型独审、用户走查 | 与生成自检重叠最强，但 check-ux 对 HTML 只查存在；用户走查非逐 AC 核对。现独审也只核 map 声明，不证明 PRD 全覆盖 | 暂留独立核对；先比较已有浏览器证据能否完整承接，再谈退役，不能把技术核查甩给用户 | 三批：待真实原型对照，未删独审；空表已退役 |
| 任务分配完整、依赖明确 | G3 前整批任务包独审 | 已默认全批一次，仅上下文超预算才按模块分；机械 AC id 覆盖不证明 oracle/API/归属忠实 | 保留整批首审；避免重复审上游已确认方向 | 三批：无进一步按包合并空间 |
| 实现正确、证据有效 | per-task 独审 | 07-08 有权限/错误传播/假绿命中；批量审会把纠错延后到多包写完。08-03 扇出判断的样本是 3:3，不能证明批量更省 | 保留逐包；已有 wave 只合末端。须取得同类任务的完整等待/整改/合并成本与漏检对照才重评 | 三批：不改逐包代码独审 |
| 选择实际检查范围 / review profile | 每次 full 的 JSON、生成器、版本/分区/继承校验 | 已是候选却仍必生成；无已核实的独立缺陷命中收益 | 用 brief 的核心项和按改动选取的检查内容替代；失去机器候选清单，范围判断由审查员负责 | 已退役生成器、专属单测、字段与校验；整合测试保留有效部分并改名 |
| 问题与处置可追溯 / status issues、comment | 逐轮报告 + status.yml 重复内联 | 原内联理由是仓内无结构化报告，已不成立；现有检查器消费报告 findings | 逐轮报告为唯一问题正文；status 留索引；失去只读 status 即可见全部问题的便利 | 已去重；历史字段原样保留 |
| 防漏 / 逐类无发现与覆盖表 | PRD/TRD/任务包/原型/接缝报告 | 空结论不能证明查过；已有清单定义应核范围 | 只报发现、必要证据与未完成验证；无发现 findings: []，完整检查职责不减 | 已取消 |
| 地基机制实际成立 | foundation 独审、正反控、最薄切片 | 07-12 记录显示只读声明曾漏判；须按当前信任前提重新看旧反例 | 保留范围内正反控及紧凑关注点证据表；不防可信作者有意绕过 | 保留，不把有用证据表当空结论删掉 |
| 基线正确、可恢复 / preflight、固定 tree、hash、报告链 | develop、审计、worktree/wave 恢复 | 与正常批次隔离、已通过任务前缀拆分有直接消费者 | 保留；合并 preflight 不能只删文件，否则可能丢写码前基线 | 本批不合并 |
| 规范使用可追踪 / standards_checked | 审查与 Standards 收缩 | 除审查外仍有独立消费者，缺字段视 unknown，不能伪作零使用 | 保留最小规则 id 接口，不恢复逐规则空报告 | 保留 |
| 组合后可达、接线完整 | 联调准备核对 + 真实路径执行 | 07-21/07-30 已有跨包缺陷与联调证据；08-09 新增独立接缝审只记录了静态回放，未找到独有命中对照 | 准备核归属、共享定义、退役；执行验真实入口/终态；既有证据有效即复用 | 二批已合并：删除独立派审/brief/专用报告，以现有联调结果承接；失去合并前独立上下文，问题发现后移到联调 |
| 业务接受度、残余风险 | 人工验收、安全敏感裁决 | 07-08 回收记录有真实拦截与安全裁决；自动测试不能替用户决定 | 保留；不把减法转化为用户重做技术验证 | 保留 |

证据来源：既有 [实证回收](../2026-07-08-empirical-harvest/findings.md)、[地基证据化记录](../2026-07-12-foundation-review-probe-tightening/design.md) 与 [方法论待议](../方法论待议.md)。这些是历史记录，不是本批模型效果实测。

## 有限验证与边界

- 普通任务：真实临时 Git 仓的 B 类完整审查，无 profile 模块/JSON、无 status issues，固定改动与报告仍可通过；Foundation 专用 brief 的报告路径同样通过。
- 重要真实缺陷的职责走查：07-08 记录的 JHH RLS 漏写 user_id → sensitive-boundaries 与必要调用链；409 被全局 filter 吞 → contract/test-evidence 与错误处理调用链；单测假绿 → 独立 oracle 与测试证据核实。三者均不依赖 profile 或 status 问题副本，仍有明确检查入口。这里只复核历史记载与现行 brief，未重跑原项目、未测实际模型检出率。
- 重要问题处置的机械验证：错任务、不可解析 tree、错 hash、累积 diff 越出 files、缺规范证据与未关闭 blocking finding 仍失败；目标已闭合及局部契约修订可定向通过，不改写旧报告。
- 中断恢复：旧 full 带 profile 引用可续新 targeted；独立报告前后链错误仍失败。真实 Git/worktree 的 wave 拆前缀通过；缺 preflight 或 commit 内结论未通过仍不能恢复为已通过。
- 兼容：不新建版本协议，旧 profile/内联问题仅保留为历史附件，当前检查器不消费；旧检查器不具备新契约，项目试点必须成组取当前规范/检查器，仍须逐仓授权。本批不改项目仓。
- 自动检查验证可执行约束，文本走查验证职责入口；两者不能证明节时或实际模型召回不变。时间/token 收益未验证。

## 第二批有限核对

- 历史样本：按 [07-21 实证](../2026-07-21-scope-vacuum-and-feedback-routing/design.md)，de-v4-009 导出入口留“后续 polish”与 de-v5-001 DOC_TYPES 调用点未收口，由准备阶段核实际承接方/调用方和共享定义；不能靠各包自报已完成。按 [07-30 联调记录](../2026-07-30-post-v1-parked-review/findings.md)，batch_no 恒 null 与九处 TRD 差异由独立契约断言/实际结果对账承接，不把实现自洽当承诺满足。
- 退役反例：待议记录的 fe-v1-005 中 TEMPLATE_ENGINE_PACKAGE 仍有两个必要调用方，准备时核注册、真实调用及保留依据，不能因 supersedes 自报删除就删掉仍检验架构边的资产。有效退役证据供后续 G5 记账复用；本批不重构 G5。
- 普通/恢复场景：无缺口只在现有 result 写一段基线/证据/结论，随后执行原路径；已有脚本也要补尚未做过的组合核对。中断后按版本差异只复核受影响项；只有 owner、未合并补缝或必要证据不足都不能进 manual-test。旧接缝报告保留，可引用其未失效证据，不新开接缝轮次。
- 限度：以上是仓内历史记录与新控制流的有限对照，不是原项目复测或模型盲测。取消独立上下文会损失第二视角，静态核对后移到合并后的联调；进入验收仍须无未解决阻断。未运行浏览器不宣称 UI 实测通过，原完整档与外部真调授权条件不变。
- 配套核对：活动文本无已删除 brief/报告生产引用；保留 global-gap-review action 作为现有补缝分流值，已改接承接方与联调，不新增枚举/协议。联调结果格式检查只校证据附件，不负责证明语义完成；未增加检查器。
