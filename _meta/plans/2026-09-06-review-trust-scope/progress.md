# 实施与验证

## 2026-09-06

- 已实施：共同信任前提及代码/地基/设计/上游审查接线，直接替换无界构造级定义、“别名可绕即未达”和持续升档要求。用户追加要求优先取消/改写旧规定，已反映到原文，不保留互相冲突的执行要求。
- 已检查：`node _meta/scripts/check-runtime-neutral.js` 通过；`git diff --check` 通过（仅仓库既有换行提示）；检查器 `check-sprint.js` 接受既有 `verified-closed`，未引入新 schema/枚举。
- 主线场景走查（文档逻辑验证，非模型行为实测）：
  - 当前代码实际违反命中 Standards：仍按规范判级，不以可信为由豁免，也不要求先有运行故障。
  - 外部请求越权、恶意文件或第三方响应触发错误：仍在范围内，必要证据不足可按 sensitive 规则阻断。
  - 未接入生产的公共接口，正常调用易造成目标错配：仍可用正常接入反例审查。
  - 修改可信 handler 故意隐藏 SQL，或仅为躲检查器构造特殊加载语法：排除，不转 backlog、不升 full。
  - 当前源码已经使用别名并违规：仍须检查；不按语法名称猜测动机。
  - 已证明根因闭合后仅出现范围外等价变体：结束追查。
  - 旧 finding 只依赖被排除假设：独立复核后沿原 id 留痕关闭，不声称代码修复、不清零轮次。
- 仅修改方法论仓。未提交、推送、跨仓分发或重判 awuchi 历史报告。原有 AGENTS.md / CLAUDE.md 用户改动未触碰。
- 局限：真实任务能减少多少审查时间需后续样本验证；本次不宣称静态走查证明了运行效果。

## 同日续修：实质减法

- 用户要求“再做减法”，本轮直接重写代码/地基审查正文；取消逐字段填表、行数自证、逐维度无发现、独立注释卫生检查和新增模块自动 full。保留紧凑地基证据表，已有未失效测试可复用。
- full/targeted 规则归并到轮次模板；局部新 finding 在 targeted 处理，广泛推翻原结论才 full。每根因三轮改为每任务累计三轮后的裁决点（第三轮已通过正常收尾）；计数沿用 report/code_rounds，不新增自动放行。检查器继续记录真实历史轮数，不把历史超三轮或已获裁决续轮当伪证据。
- profile 为候选，审查员可据事实排除/补查并简记理由；生成 JSON 不可改写。选择器识别 shared/fullstack；空 api-contract 不触发输入维度；comment-hygiene 合并到适用规范。
- 兼容：同一 profile schema 新输出 selector_revision=2，历史缺字段按修订 1 逐项重算；保留旧 dimension id。未知修订拒绝，不会因升级误报历史 selected/omitted。
- 减量：本轮开始四份核心文件（develop-review/foundation-review/develop-review-round/review-scope）约 15,210 字符，修改后归一换行为 6,562 字符，约减少 57%；这是正文载荷，不是实测工时/token。执行规范同步去除旧 full 触发条件。
- 验证：选择器单测、审计集成测试、运行时中立检查与 diff check 通过；新增覆盖 shared 精确选择/真实 UI 输入并发风险保留/空 API/旧 profile/未知修订/局部新 finding 闭合和未闭合。额外取 HEAD 原选择器回放四种 layer，历史修订结果一致。首次回放脚本遗漏 Module filename 参数导致 Node 断言退出，修正验证脚本后通过，未改产品逻辑绕过失败。
- 本轮为本仓实现；未提交、推送或分发。AGENTS.md / CLAUDE.md 原有改动未触碰。

## awuchi 新样本与穷举复议

- 取消 fullProfileErrors 按最新任务文本重算历史 fingerprint/signals/候选维度的校验；保留 task id、固定文件集、risk、schema、分区和核心项检查。历史 standards_checked 不按最新任务表回改，末轮仍核当前规则。profile 是历史候选，最新契约由审查员定向核；不新增 reconciliation 产物。
- 回归新增：任务 title/规则归属修订后，旧 full + 新 targeted 能闭合且 profile 字节不变；错 task-id、错固定文件集、未闭合 blocker 仍失败。选择器、审计集成与中立检查通过。
- 恢复直接复用原审查链、对象和下一个编号；此为执行规则修正，检查器只核可见报告链，没有新增跨 worktree 扫描器，尚未实测真实跨工作树恢复行为。
- 用户提醒穷举具有双面价值，故修正“逐字段验证都是重复验证库”的过宽推断：awuchi 003 的 99 字段测试可能验证项目必填配置，是否冗余取决于期望是否来自独立契约、已有证据是否充分，不能仅凭测试数判浪费。
- 使用点与处置：AC→TRD→任务包完整性、受影响 DTO 输入去向、地基关注点、UX 不同业务分支、联调不同终态、安全敏感输入拼接点均保留完整核对；无须穷举所有输入组合或路径排列。已有机械结果可复用，语义承接仍需看内容。当前未对 UX/联调执行规范另作改造。
- 最终测试措辞：独立契约下有限字段/状态/约束可参数化遍历；项目漏字段/错设可选/接线错误仍检查；消费者不重复未变共享 schema 全套测试，不强制逐项写报告。不新增穷举专项流程或新 Gate。
