# 方法论待议

- [ ] 2026-05-10 | Gate 判断标准统一：hact-app 当前靠文档文件（prd_path/test_report_path 等）判断 Gate 签署，而 CC 写 gates.md；两者脱节导致 UI 与 CC 状态不一致，且文档命名与规范（acceptance-report.md vs test-report.md）存在歧义。建议改为 sync 解析 gates.md 直接驱动 iteration gate 字段，彻底消除耦合。| 来源：hact-app v3 feedback
- [ ] 2026-05-31 | 设计乙（个人 notes 即时生效扩展）：当前双源规范只在 draft-tech-design 生成端注入执行人 notes `[规范]`（设计甲），依赖"架构≈开发"高度重叠。团队分化（架构与开发分离）后，develop / pr-review 加载端是否也叠加加载本人 notes 以即时生效，待评估。| 来源：personal-notes-accumulation 方法论调整
- [ ] 2026-05-31 | checklist 双源对称（O1）：standards 已双源（生成端注入个人 `[规范]`），但 checklist 不由 draft-tech-design 生成，本人 `[checklist]` 当期不生效、只能经 harvest-notes 上提后下期全队用。是否在 develop 自检加"过一遍本人 notes `[checklist]`"，与设计乙同根，待团队分化一并评估。| 来源：personal-notes-accumulation 方法论调整
- [ ] 2026-06-16 | status.yml 写入校验（与 hact-app sync 切读 status.yml 迭代捆绑做）：**当下不做**——校验实读 hact-app `backend/src/sync/sync.service.ts` 确认它仍解析 markdown（queue/sprint.md/gates.md）、不读 status.yml，且 hact-app 仓里无 status.yml 文件。即处于"新老方法并行阶段"，status.yml 是"只写没人读"，写坏也无消费方受影响，写入校验价值要等 hact-app 切读 status.yml 才兑现。届时连同消费切换一起上以下设计：(1) 仓内固定 **Node 脚本** `scripts/check-status.js`（贴技术栈、调用稳、避免手敲 python/node 命令的跨平台脆弱与"校验器自身报错被误判为 YAML 错"），做 **schema 校验**（枚举值/date 正则/必填键/schema==1）而非仅语法 parse——因 hact-app 对脏枚举是静默降级为 todo（`normalize()`），语义错比 parse 失败更隐蔽；(2) 分级：简单 gate bool 翻转不校验，只在 tasks[]/integration_tests[]/code_reviews[] 等数组复杂写入时校验 + 读回关键字段确认；(3) 进 `skeleton/07-status-contract.md` §五写入纪律统一声明"凡 `git add status.yml` 前先过校验"，各 exec spec 引用不重复；(4) init-project 负责铺脚本 + js-yaml 依赖。注：parse 失败的降级/告警是 hact-app 侧责任，hact-method 只管"尽量不写坏"。| 来源：2026-06-16 method-adjustment（2.1）
- [ ] 2026-06-16 | status.yml 接续一致性核查（与 hact-app sync 切读 status.yml 迭代捆绑做）：**当下不做**——拟在 `templates/CLAUDE.md` Step1 推断前对比 status.yml.gates ↔ gates.md、status.yml tasks ↔ sprint.md，不一致则报告等人工确认。但其本质是 status.yml↔markdown 对账，处于"新老方法并行阶段"（status.yml 只写没人读、CC 接续推断读的是 markdown）时对账它是空转。与 2.1 一并延后到 hact-app 切读 status.yml 时做。残余的"CC 接续时 markdown 自身半写入检测"单人阶段价值薄（CC 读自己写的文件），暂不投。| 来源：2026-06-16 method-adjustment（2.2，A-P0-2 + A-P2-6）
- [ ] 2026-06-16 | 共享资产依赖完备性（团队期）：本轮已把 `depends_on` 补进任务包字段（清 P1 脱节），消费方靠反查。但"多任务并发改同一共享表/枚举/类型、漏标 depends_on 边"在团队期才真正咬人。待团队引入后，给 plan-sprint Step3.5 独审增第⑤类「共享资产依赖完备性」——交叉比对各任务包 `files`，≥2 任务碰同一共享文件/表/枚举则校验 depends_on 边标全（files 字段结构化，机械可查）。单人串行暂靠 Step2.5 交付判断 + 拾取顺序兜。| 来源：2026-06-16 method-adjustment（1.7）
- [ ] 2026-06-16 | develop 选读 decisions.md（防重新发明被否决方案）：当前 decisions.md 是「决策/原因/日期」结构、无 files/模块映射键，develop 无法机械判定"哪条 decision 与我的 files 相关"，且 decisions.md 当前空表、单人阶段无积累否决决策，风险未实际发生。待 decisions.md 有实际积累 + 补"涉及模块"列后再议；非做不可时改由 plan-sprint（本就读 decisions.md）把相关否决决策摘要写进任务包 `do-not`，不让贫信息的 develop 侧去 files-match。| 来源：2026-06-16 method-adjustment（1.7 主张 B）
- [x] 2026-06-16 | pr-review 批量时机规则对串行依赖链的表达缺口：pr-review.md §会话启动写"同层全部任务推 PR 后批量审查"，但未区分**并行独立任务**（可真正批量）与**串行依赖链**（A→B→C 各为独立 PR，B 依赖 A 合并到 master）。后者无法等全部推完再批量——B 分支必须从 master 切，A 未合并则 B 的代码在 A 分支上，形成 stacked PR（被 develop.md 禁止）或分支污染。develop.md 已通过阻断条件（「独立 PR 未合并则依赖它的任务不可拾取」）在执行层强制了逐个合并，但 pr-review.md 批量时机描述与此隐含矛盾。建议在 pr-review.md §批量时机补注：「串行依赖链中的独立 PR（A 是 B 的 depends_on）须逐个 review 合并，不受同层批量等待限制；批量等待仅适用于互不依赖的并行独立任务。」| 来源：org-krm-v2 v5 backend 串行链（001→002→{003,004}→005）实操反馈
- [ ] 2026-06-16 | relevant-standards 覆盖度扶正为真机械判据：plan-sprint 字段保真自检目前对"relevant-standards 是否覆盖 files 所属强制规范"靠语义判断（移交 Step 3.5 独审），因 standards 模板无"文件类型→强制规范节"映射表，机械查不了。若团队引入后文件命名规范化（可按 pattern 归类），可在 `templates/standards/backend.md`/`frontend.md` 顶部各加一张"文件类型→规范节"映射表（如 `*.controller.ts → §响应格式+§入参验证`、`*.vue → §设计系统+§组件`），届时这条即可从独审语义判断升级为自检机械硬卡。单人阶段 ROI 低，暂不做。| 来源：2026-06-16 method-adjustment（1.3 字段自检升级保真，替代 B）
- [x] 2026-06-18 | 第①类「真·同步滞后」的 Step 0 改造：项目仓 `templates/CLAUDE.md` Step 0 的 `git -C ../hact-method pull 2>/dev/null || echo "已同步"` 会吞掉"本地不在 master / 凭据失败 / diverged"等错误、还输出"已是最新"的假成功信号。改：去 `2>/dev/null`、显式校验当前在 master 分支、"已同步 N 个 commit"声明改为基于 `git rev-parse HEAD` 前后对比的真实结果（拉到才说拉到）。与本轮 Gate 冷核（第②类）根因不同、文件不同，另起线。| 来源：2026-06-18 gate-criteria-cold-check 推演（第①类）<br>**2026-07-11 已落地**：Step 0 改为 `sync_repo` 函数——不吞 stderr、`pull --ff-only`（diverged/冲突/凭据失败原样报错）、hact-method 显式校验在 master、拉取数由 HEAD 前后 `rev-list --count` 实测；声明纪律补"失败不得改写为已同步"。项目仓不限分支（断点续做可能在任务分支）；harvest-notes 的 `2>/dev/null` 是 clone 兜底路径、不在此列。五情形 + 端到端已实测。
- [ ] 2026-06-19 | 【全局方向】质量模型从"规范遵循验证"转向"输出可测试性"：当前方法论在规定输入侧投入最多（规范/冷核/Gate），在输出验证侧最薄（仅末端 integration-tests + manual-test）。方向性转变：每个阶段的产物应定义"如何自动验证正确性"，而非只定义"格式应该是什么"。具体落地路径待讨论，可能影响 Gate 机制重新定义、specs-structural 完成判据从清单改为可运行检查。完整分析见 `_meta/plans/2026-06-19-structural-review/findings.md` §四。| 来源：2026-06-19 结构性审查
- [ ] 2026-06-19 | develop 拆分为多个 task type：source=sprint/integration/manual-test/bug/optimization 五条路径上下文加载/反馈去向/checklist 完全不同，强行合并进一个471行规范。拆分后每个 type 的规范可大幅压缩，同时消灭"按属性分支在运行时路由"的解析错误风险，缓解 context 压力。影响：specs-structural/develop.md 拆为多份、specs-execution/develop.md 拆为多份、task type 枚举扩展。| 来源：2026-06-19 结构性审查 #1
- [ ] 2026-06-19 | B 类任务升级触发检测（"是否修改共享契约"）：当前 B 类分类标准是"来源（bug/optimization）"，真正边界是"是否修改 TRD/standards 等共享契约"，两个维度不等价。一个 B 类优化任务实际改了接口契约时，它绕过 G2/G3 保护、无任何机制检测，在真实项目中必然发生。在 dispatch-new 或 develop 入口增加"是否改动共享契约"的自检触发，命中则提示升级为 A 类 revise-doc 路径。| 来源：2026-06-19 结构性审查 #6
- [ ] 2026-06-19 | TRD shared-type-first 架构（解锁并行化前提）：TRD 按 API 模块并行生成是降低 context 压力的有效方向，但各模块共享数据模型（User/Dept/Task 等实体）。解法：在 API 模块生成前，先跑"共享类型定义" subagent 产出 schema.md，所有后续 API 生成 subagent 引用它。这是 generate→validate loop 在 TRD 阶段落地的前提。| 来源：2026-06-19 generate→validate loop 分析
- [ ] 2026-06-22 | 【前端规范一致性框架落地】：JHH-Nortion 审计暴露两个结构性失效（规范无防线 + 注册表不可信）。方向已讨论清楚，需在具体项目（JHH-Nortion 或 hact-app）落地以下五件事：① lint 规则（禁 hex 字面量、禁 `*Labels`/`*Options` 本地重声明）挂进 pre-commit-hook；② `scripts/check-reusables.js`（验登记路径存在 + 废弃资产无引用）；③ plan-sprint 任务包模板增加"复用决策"字段（UI 元素 → 用哪个现成组件 / 新建并登记）；④ plan-sprint Step 3.5 独立审查扩展复用决策 + 交互模式决策两个维度；⑤ TRD 接口设计段增加交互模式规格字段（防抖时长、loading 态、错误处理）。完整框架见 `_meta/plans/2026-06-22-frontend-consistency-framework/findings.md`。| 来源：JHH-Nortion 组件复用与规范一致性审计 + 2026-06-22 方法论讨论<br>**2026-06-28 部分落地**（hact-app v8 视觉低级错误触发）：本框架缺的「视觉地基层」（跨切面公共件无人认领，R2/R3）+ token 落地门禁（R1，落 develop lint 层）+ 视觉冒烟断言（R4，前移到 generate-integration-tests）三件已落，见 `_meta/plans/2026-06-28-visual-baseline-package/`。**剩余仍待**：① 组件复用 lint（禁 `*Labels`/`*Options` 本地重声明）② `check-reusables.js` ③ 任务包「复用决策」字段 ④ Step 3.5 复用 + 交互模式维度 ⑤ TRD 交互模式规格字段。
- [ ] 2026-06-22 | 【后端规范一致性框架落地】：与前端框架同源（同一 findings 文档），后端三维度为：规则层（响应格式/DTO 校验位置/错误抛出方式）→ TypeScript strict + ESLint；复用层（分页 helper/过滤器/通用 DTO/查询工具）→ reusables.md 后端区块 + check 脚本；模式层（软删除/事务边界/批量失败处理）→ TRD 接口设计段 + 任务包。核心与前端的两个差异：① 后端规则层机械化覆盖率更高（无视觉判断盲区）；② 后端多一个主力机制——AC → Given/When/Then → 可跑测试，直接验行为正确性而非规范遵循。需落地五件事：① TypeScript strict + ESLint 自定义规则（响应格式、DTO 校验、权限守卫）挂进 pre-commit-hook；② reusables.md 增后端区块 + check-reusables.js 覆盖后端路径；③ TRD 接口设计段增 Given/When/Then 规格字段；④ 任务包模板增测试要求字段（来自 AC 精化）；⑤ hact-method 补技术栈子模板层（`templates/standards/frontend-vue3.md`、`templates/standards/backend-nestjs.md` 等），当前模板只含结构提示。完整框架见 `_meta/plans/2026-06-22-frontend-consistency-framework/findings.md` §七-十。| 来源：2026-06-22 方法论讨论（前后端规范一致性）<br>**2026-07-08 ⑤已落地**（决策#27 技术栈剥离）：栈子模板层建成（`frontend-vue3.md` / `backend-nestjs.md`，通用模板瘦身为栈无关原则），standards 播种双源升三源（通用 + 栈子模板 + 个人 notes），skeleton/specs 去栈措辞。①-④ 仍待。
- [ ] 2026-06-22 | 【自动化水平提升·GUI 笨宏换窗续设计】：L3 编排层（站到站推进、换窗续跑）仍全人工。已讨论清楚的方向：本地确定性宏（AutoHotkey/pyautogui）监听 flag 文件 → 开新窗敲恢复词，绕开 Credit Pool 保留 Max 订阅。待续设计的命门：**CC-1 换窗前必须往交接棒文件写哪些东西**，才能让 CC-2 读文件后完全自恢复不丢线。附带已拍板的两个决定：① G2/G3 纯技术判断可并入中段无人区；② manual-test 移出 G4，作为 owner 意图闸与 G1/G5 并列。完整讨论见内存 `project_automation_gui_handoff.md`。| 来源：2026-06-22 `/remote-control` 闲聊
- [ ] 2026-07-08 | checklist 栈子模板对称拆分：决策#27 已把 standards 拆成「通用 + 栈子模板」双层，但 `templates/checklists/frontend-checklist.md` 仍内嵌栈特定项（vue-tsc / Element Plus 模板禁忌 / stylelint 规则名）。当前靠 checklist 自带的"诚实前提"（项目未配对应规则的项落留人走查）兜底，拆分收益低，暂不做。待栈子模板经真实项目验证 + 出现第二个栈的项目时，按同一模式拆 `frontend-checklist-{栈}.md`。| 来源：2026-07-08 技术栈剥离（决策#27 遗留项）
- [ ] 2026-07-10 | hotfix 前端设计门减重（⚖️ 化）：B 类 hotfix 若**无新增视觉/交互形态**（只改已有 UI 的业务逻辑/数据限制，不新增组件/页面/交互模式），前端设计覆盖检查从人工门降为 ⚖️ 默认判定——CC 自判"无新增视觉形态"并输出结论+理由后直接续跑，可推翻。符合决策#28 分级模式，与运行时无关（CC/Codex 同受益）。受 2026-07-08 机制冻结约束，暂不落正文；冻结解除或下次被打疼时按 develop.md 前端设计覆盖检查处加一个 ⚖️ 分支落地。| 来源：2026-07-10 JHH-Nortion jhh-b-004 Codex 试点反馈（活动挂接父级互斥修复，纯业务限制、无新视觉形态，设计门体感偏重）
- [ ] 2026-07-11 | pre-commit 门卫对 init 提交误报（check-sprint 路由过宽）：init-project 首次 commit staged 了 `iterations/v1/queue/done/.gitkeep`，`pre-commit-hook.sh` 据路径路由到 `check-sprint.js`，报"queue 目录无任务包"拦截——而立项时 queue 必然为空（任务包到 plan-sprint 才产生），任何新项目 init 提交都会被拦，只能 `--no-verify` 绕过。修法候选：门卫路由排除 `.gitkeep`，或 check-sprint 对"queue 仅含 .gitkeep"视为初始态跳过。受 2026-07-08 机制冻结约束暂不改模板，解冻后落地。| 来源：2026-07-11 doc-extract init-project 实操<br>**2026-07-11 已落地**（用户点名修 bug，属既有机制纠错、非新增机制，不违冻结）：取路由方案——`pre-commit-hook.sh` 的 check-sprint 触发从 `queue/` 收窄为 `(sprint\.md|queue/.*\.md)$`，`.gitkeep` 不再触发；check-sprint.js 本身保持严格（显式调用时 queue 空仍 FAIL，G3 判据不松）。init 提交放行 + 真任务包提交仍拦截已实测。注意：已建项目仓（doc-extract 等）的 `.git/hooks/pre-commit` 是复制件，需手动重拷一次模板。
- [ ] 2026-07-12 | G3·机械级是否要求"接进自动门"：foundation-review 认证机械级时，检查只要在配置里存在（`.stylelintrc.json`/`eslint.config.mjs` 有规则）算达标，还是必须接进一道**真在自动跑、能拦坏代码进 master 的门**（CI/pre-commit）？doc-extract E5 实例：stylelint 规则在配置里但仓库无 CI，红线"只活在本地手敲命令"，图省事的人不跑 + 无 CI → 违规照绿进 master，实际滑回人审级。**本轮搁置理由**：① G1+G2 已吸收大半（规则有洞→G1 撞、检查没跑→G2 自绿复现暴露）；② 本方法拦 master 的门本就是 develop 自绿那一步、非 CI 服务器；③ 纯残余="必须另有常驻 CI"=新增基建要求，撞 2026-07-08 冻结。**落地候选**：Gitee Go 流水线 / pre-commit 钩子（本仓 `check-sprint.js` 门卫已有先例）/ develop 自绿即算。**重启条件**：价值随第五阶段真人 push 上升——真人 commit 不走 develop 自绿，那时 CI 才承重；与决策#24"团队引入期若需恢复独立人工 merge 门可再评估"同一时点一并重评。| 来源：2026-07-12 doc-extract V0 二轮独审 E5 + foundation-review 证据化收紧讨论（`_meta/plans/2026-07-12-foundation-review-probe-tightening/`）
- [ ] 2026-07-12 | V0 存在时 prep 阶段缺 as-built 对账（TRD/plan-sprint 照理想模型切包 ≠ V0 实况）：doc-extract V1 `de-v1-004` 执行 subagent 直接 blocked、0 代码落盘、浮 3 处设计裁决 + 另起 `de-rd-001` 对账任务——吃掉 develop ~27% 墙钟并拖尾 005/006/007（"对账族"）。根因=TRD 状态词汇/枚举（`extract_failed`、进行时分词）+ 抽取队列 + pipeline_transition 边 `enabled` 清单 与 V0 实际库（`status.yml`/`foundation.md`/迁移）不符，`draft-tech-design`/`plan-sprint` 未对账 as-built 地基。**是唯一漏网的独立病因类别**：B 类 P1–P5（修"声明档>实测档"强度洞）在本轮 develop **前**已全合、foundation-review 探针收紧也已落，**两道防线都盖不住它**——它不是"墙不够硬"，是"V0 按走骨架设计只建半条管线（一条边/一个队列/无 PDF 库）+ TRD 没对账实况"。时间线钉死其家=`draft-tech-design`：B 类 07-11 19:22 全合 → TRD/G2 21:02（在其后）→ TRD **仍**带错的状态词汇，即 as-built 早在位、是写 TRD 时没读实际库枚举（`plan-sprint` 忠实照 TRD 切包、非其错）。**落点**：`draft-tech-design` V0 存在时增一步"TRD 状态词汇/枚举/队列/pipeline_transition 边 对账 as-built 地基（读 status.yml/foundation.md/迁移）"。**搁置理由**：doc-extract 是首个走完 V0→V1 的项目，单样本不足立机制（守 2026-07-08 冻结 + empirical-harvest measure-first；记录≠部署）。**重启条件**：第二个 V0→V1 项目复现，或下个项目 `draft-tech-design` over 存量 V0 时顺势落。| 来源：2026-07-12 doc-extract V1 develop 耗时归因（de-v1-004 阻塞 escape-hatch，墙钟 7.3h 中最大单点）<br>**2026-07-28 重挂**：doc-extract 转入重建、不再迭代，原条件改由**重建后的新项目**承接（它会完整走一遍 V0→V1）。同时本条在 doc-extract 拿到了比"状态词汇对不上"更强的形态——同一 V0 关注点跑到 v6，`pipeline_transition` 累积 17 条边含三组环，"看似顺序生命周期实为可回退图"，无人负责复核整体是否仍自洽。落点因此扩为两半：① 原有的 TRD↔as-built 对账 ② 已登记地基关注点的**自洽复核**（设计稿 `2026-07-28-foundation-concern-coherence/`）。
- [ ] 2026-07-12 | GIT（generate-integration-tests）价值随 AC 链+独审前移而"挪位"、现瞄错靶：上游（行为化 AC 链 + per-task 对抗独审跑真库全量+反例）已清"装配整体跑通吗"，幸存 bug 只剩三缝——(a) 跨模块接缝 (b) 真实外部边界 (c) 真浏览器/真数据渲染。现状 GIT 测**安全中段 + mock 外部**、三缝都没碰 → 不仅低命中（harvest 2/118 + doc-extract v1 后端 0/31），更是**低信息量**（全绿≈无信息：测的地方本就不会错，全绿不能证 seam/boundary 成立）。**两半不对称**：① **GIT-API = 瞄错靶、可回收**——改成"一条真**全管线驱动穿透**（本项目会当场撞 de-v1-004 类'边 enabled=false/状态词汇'）+ **真外部边界冒烟**"，信息量即回（= empirical-harvest 已挂的改造候选）；② **GIT-web = 与必留的 manual-test（owner 闸、砍不掉）高度重叠**——v1 因环境无浏览器/pinchtab 直接降级并入 manual-test（静态 token 锚点核对 + develop 单测兜底，非静默通过、决策#28 ⚖️ 正确姿势），首次发现被 manual-test 支配，残余仅"自动回归 + 真数据形态"（harvest 浏览器回归逃逸 ≈0）。**证据强度不等**：GIT-API 低价值=2 点实证(2/118+0/31)+信息量论证，**较硬**；GIT-web 被 manual-test 支配=理论+harvest 视觉后移，v1 那次系**环境强制降级、非 by-design 对照**，属**弱支撑，需一次 browser-capable 环境真跑才够判**（另：若真实开发机也常跑不了 pinchtab，可用性低再记一记）。**落点**：`generate-integration-tests` 改造——API 转"穿透+边界"、web 侧正式化"并入 manual-test + 静态锚点代偿"为合法路径而非降级。**搁置理由**：GIT-web 欠一次真跑；守 measure-first + 2026-07-08 冻结（改造既有机制非新增）。**GIT-API 改造已 land**（2026-07-12，改 `specs-execution/generate-integration-tests.md` Step2 穿透替矩阵 / Step3 按流 / Step4 ⚖️ 双路由 / 新增 Step4B 边界闸 + `specs-structural` 字段规范·完成判据）：穿透替矩阵·数量按终态原则·失败 ⚖️ 双路由；边界闸=桩点即边界 ⚖️ 探测 + 🚫 真调 + 临时样本 + 非静默降级。设计稿 `_meta/plans/2026-07-12-git-api-reshape/design.md`。**下次 GIT 运行即改造自身第一手验证**（穿透是否比矩阵多抓 / 边界闸是否真拦外部接线）。**GIT-web 仍待**（欠一次 browser-capable 环境真跑再定要不要瘦身，web 侧本轮不改）。<br>**2026-07-28 取数窗口已开**：doc-extract v6 联调在会话内自备浏览器（playwright-core + Chromium，不入仓）跑通真登录与四条新路由，**这就是本条欠的那次 browser-capable 真跑**——v6 联调结束即收割：前端档实际拦下几条、与 manual-test 重叠多少。窗口一次性（doc-extract 此后不再迭代），错过则本条只能改挂重建后的新项目。| 来源：2026-07-12 doc-extract v1 GIT 结果（后端 31/31 全绿 0 命中 / 前端环境降级并入 manual-test）+ 理论分析，承 empirical-harvest generate-integration-tests 改造候选
- [ ] 2026-06-18 | Gate 冷核硬化（B3 hook + Layer A 上游防）：本轮只落地 Gate 完成判据冷核**软版**（规范步骤 + 凭证 + 人工抽看，无机械防线）。后续增强：(B3) `PreToolUse` hook 守签字动作，凭证缺失/过期/产物指纹不符则拦截签字（一个 hook `G{N}` 参数化包全 5 Gate，由 init-project 铺进项目仓）；(A1) 完成判据字段→产物模板空槽（PRD/TRD 预印 `入口:`/`draft-ux:` 槽位，空着即刺眼）；(A2) 机械 linter（Node，字段缺失/空段确定性报错，可与待议 `check-status.js` 合并）。软版扛不住"主会话整步跳过"和"把 FAIL 洗成 pass（六种伪造）"，硬化才补机械闸。完整设计见 `_meta/plans/2026-06-18-gate-criteria-cold-check/design.md` §二/§三。| 来源：2026-06-18 gate-criteria-cold-check（B3 + Layer A 延后）
- [ ] 2026-07-21 | GIT 穿透流实际命中率待第一手验证（B2「新增全局接缝审」是否必要的前置）：doc-extract v5 二次全面独审出 8 阻断，报告据此主张在「develop 全 merged 之后」新增一道全局接缝审步骤——但核实 `iterations/v5/gates.md` G4 未签、`integration-tests/` 无 `scripts-v5.md`，**v5 根本没跑 `generate-integration-tests`**，而 GIT-API 已于 2026-07-12 改造成"穿透 + 边界"、位置正是该槽位。逐条比对 8 阻断：B3（改判端点窗口为零——报告自陈 happy-path 靠夹具把记录留在 `registered` 才绿，正是穿透流红线「禁 fixture 抄近路」所禁）、B1（提单类型三条实操路径证伪）**穿透流按现有判据应当撞上**；其余 6 条（TOCTOU 并发 / 双渲染 / mergeChunkedFields / 前端三条）穿透流看不见。**结论：不是缺一步，是已有的那一步没跑 + 进料口可能偏窄。** 落点候选：扩 GIT 穿透流进料口（如"上期能力回归"一条流），远比新增一步便宜。**重启条件**：v5 补跑 GIT 后，比对穿透流实际拦下几条 vs 二次独审的 8 条，用真实数据定。承 2026-07-12 GIT 改造条目「下次 GIT 运行即改造自身第一手验证」。<br>**2026-07-28 重挂**：v5 不会补跑（doc-extract 转入重建、不再迭代）；改挂 **v6 的 GIT 运行**——9 条后端穿透流已生成待跑，跑完即比对"穿透流实际拦下几条 vs v6 全面独审的 finding 集"。同为一次性窗口，与上一条同批收割。| 来源：2026-07-21 doc-extract v4/v5 经验总结核对（`_meta/plans/2026-07-21-scope-vacuum-and-feedback-routing/design.md` §一）
- [ ] 2026-07-21 | 地基反转类迭代的「旧假设回查包」（报告 B1，真·新增机制、守冻结）：v4（一文件=一单证 → 拆多段）与 v5（类型是代码 → 类型是配置）都是**推翻早期全局假设**的迭代，两期最大残留都源于"全仓仍有代码依赖旧假设、但没有任何包被指派去找它们"。提议：这类迭代强制出一个不写新功能的回查包（任务=全仓找依赖旧假设的残留）。**搁置理由**：新增 task 形态、撞 2026-07-08 冻结；且与本轮已落地的 A2 归属真空检查部分重叠（后者查"包与包之间"，前者查"包与历史之间"，先看 A2 跑一期能拦多少）。**重启条件**：下一个地基反转类迭代先**手工做一次不入规范**，跑完看产出再决定是否立机制。| 来源：2026-07-21 doc-extract v4/v5 经验总结（报告 B 类 B1）<br>**2026-07-28 重挂**：改挂 doc-extract **重建后的新项目**——整个重建即一次最大规模的地基反转（重定义状态模型与编排契约、剥离双引擎），其阶段 0/1「盘点旧实现哪些依赖旧假设」正是本条要手工做的那一次。新项目跑完看产出再决定是否立机制。
- [ ] 2026-07-21 | AC 可达性自检 / 后端验收锚 / 豁免双侧实证（报告 B3-B5，checklist 类）：① **AC 写作自检**——凡 AC 出现容器/列表/状态名（"在复核队列可见""处于可重抽态"），须回答"该记录此刻在什么状态？到目标位置有没有边？"（v4 AC-02 + v5 AC-06/AC-30 三次复现，v5 AC-30 实测状态窗口为零）；② **后端任务验收锚**改为"前端能用它做成事"而非"我实现了 AC"（v4 frontend 连撞 3 处后端契约缺口 / v5 前端下拉列出提单而后端常量白名单拒它）；③ **豁免文本双侧实证**——凡"仅在 X 成立 / 仅 Y 不成立"类边界断言，独审须验两侧（v5 B2 的豁免把双渲染认证为合规路径；一条写错边界的豁免比未发现的缺陷更有害，它让人主动停止寻找）。**搁置理由**：三条都是 checklist 类增补，需等 A1 的载体（backlog `[欠账]` 链路）跑过一期确认不再被倒掉，否则写进去也是下次 wrap-up 被清。| 来源：2026-07-21 doc-extract v4/v5 经验总结（报告 B 类 B3/B4/B5）
- [ ] 2026-07-29 | 矛盾当场闸 + 删除授权（退役账缺的另两半）：2026-07-28 落地的**退役账**（任务包 `supersedes` → develop PR 逐条结账 → wrap-up 签 G5 前核对）只解决了「记账义务」，漏两处——① **事后 vs 当场**：退役账在 PR/G5 才结，而复杂度是在「实现与黄金样本/已签契约冲突」的那一刻长出来的；现有 `escalate-if` / escape-hatch 管的是"上下文不足以决策"，**从没有一条规则说「兼容层 / 双路径 / 临时状态 / 兜底分支」本身就是失败模式**。拟落：冲突时**立即停止**、禁止用「两个都保留」收场，出冲突报告（冲突双方 / 实际调用证据 / 黄金样本影响 / 建议保留 / 建议删除 / **不裁决时会新增什么复杂度**）交人裁决——最后一栏是强制函数，逼着在决策当下把省事那条路的代价写出来（doc-extract 源码 130 处「回落」，每一处当时都便宜，没有任何一步要求算这笔账）。② **有义务没权限**：agent 发现死代码只能申报、无授权动手，于是进 backlog 后没有然后。拟落授权边界：授权范围＝**工作副本中与已确认契约冲突的旧实现**（不授权删生产库 / 原仓）；删前须引用扫描 + 黄金样本验证 + 影响说明；人裁决「保留 A 删 B」后**同一任务内**删 B 及其测试/适配器/死分支，不许拖期；**禁止改名 `legacy`/`fallback`/`compat` 后继续保留**（本仓判据现为「路由为零但仍注册 = 保留不是下线」，说的是同一件事但没堵改名这个逃生口——doc-extract 旧 `QwenEngine` 正是靠"休眠退役"四字活下来）；临时兼容须经批准并记负责人 + 删除条件 + 最晚删除阶段，否则默认数量为零。**搁置理由**：守 2026-07-08 冻结；且重建项目将先按此手工跑一遍（同 #28「旧假设回查包」的姿势：先手工做一次不入规范）。**重启条件**：重建项目跑完，看冲突报告实际产出几份、删除授权实际用掉几次，再定是否升成规范。| 来源：2026-07-29 对重建计划的第三方审查意见（用户转述）+ 2026-07-28 doc-extract v1–v6 代码审查（增生无退役）
- [ ] 2026-07-30 | develop 阶段 B 独审墙钟压缩（扇出并行）——**先量后改，最小变体是"摘最重一类"而非蜂群**：现象=开发 ~20min、独审 40–60min，独审成主要墙钟。两条并行轴不等价：① **跨任务并行**——任务图层面成立（`develop.md:107` 串行的理由是「串行单工作树无写冲突」，管的是**写**、不是只读的审查），但机械上卡在"可并行任务集共用一条 batch 分支、主循环期间不 commit"（commit 在末端一次），`git diff` 是累积的未提交改动，真并行则 A 的审查员与 B 的执行体同时读写同一棵工作树、**审查对象随写入漂移**；解锁前提=batch 分支上 **per-task commit**、审查读 `git diff {prev}..{cur}` 的钉死快照（该步本身独立成立，也让现有串行下"审 B 时 diff=A+B"更干净）。② **单任务内按维度扇出**——`develop-review.md` 八类的输入集基本不相交且成本极不均（第 7 类 DTO 逐字段穷举最重 > 第 6 类设计保真 > 1+4 AC/测试 > 2/3/5/8 轻），切三份并行则墙钟由 sum 变 max；且与 2026-07-12 foundation-review 的结论同向（「brief 严苛度 > 模型大小」＝瓶颈在注意力/指令而非算力，八类塞一个审查员本就在稀释注意力）。**代价（不粉饰）**：(a) 省时不省钱——共享输入 ×N 重读，正面顶决策#26 成本原则，是拿 token 换墙钟；(b) 跨类 finding 会漏（典型：测试忠实 AC 但该字段在 service 被覆盖，横跨第 1 与第 7 类）→ 切分粒度须让每个审查员握 2–3 个相邻类，不可一类一员；(c) 整改 loop 变糊——N 份清单合并后重审跑几个？只跑报阻断的丢了回归、全跑成本再 ×N，须先定规则（如"整改若改动超出该审查员 scope 的文件则全跑"），现有 3 轮封顶随之要重新界定；(d) 误报叠加——brief 写死"存疑即判阻断"，N 个偏保守审查员的假阳性是叠加的，正是 2026-07-14 haiku 回调那个坑换了形状。**最小变体（若要动，先只做这个）**：只把**第 7 类（用户输入去向追踪）**摘成一个并行审查员、其余七类留原审查员——单类最贵、输入集最独立、且输出**自带覆盖率自证**（`行数 < N = 审查未完成`）机械可核、误报面窄；一刀换掉大半墙钟而结构改动近零，(b)(c)(d) 三条代价基本躲开。全面扇出（3–4 员）留到跨任务并行也想要时一并做（那时 per-task commit 反正得补）。**搁置理由**：守 2026-07-08 冻结 + empirical-harvest measure-first——这 40–60min 里"首轮审查"与"整改—重审轮次"各占多少尚未量过；若大头是 2–3 轮 loop，则扇出首轮基本白干，串行的整改才是关键路径。**重启条件**：先在下一个真实 develop 会话里拆出「首轮审查耗时 vs 整改重审轮次耗时」占比（同 doc-extract develop 变慢那次的姿势：先量出"7.3h 里 1/4 是一次性地基债"再判方法论有没有病）；**仅当首轮占大头**才推进最小变体。| 来源：2026-07-30 对话（用户提"审查能不能派蜂群扇型铺开"）
- [ ] 2026-07-29 | Complexity Gate（阶段级复杂度维度）——**思路可取、六项自填不可取**：审查意见提议给每个阶段 Gate 加一道复杂度门（新增纯兼容分支 / 未裁决契约矛盾 / 未批准边界例外 / 重复解析器·状态判断·提示词实现 / 无删除条件的临时开关 / 已被替代仍残留的旧路径，**六项全须为 0**）。**Gate 加复杂度维度是本仓没有的**（现有五关全是正确性/完成度判断），值得记；但**若六项由执行方自填即恒真式度量**——执行方既定义什么算"兼容分支"又统计有几条，只能回答"我列的都归零了吗"，回答不了"我列的是不是全部"（`2026-07-22-evidence-burden-shift` 讨论盘第二层 + 决策#29「risk 不信自报」+ 独审不收执行方自评，本仓已反复付钱）。**要用须改造**：分母来自覆盖方控制不了的独立枚举源——「重复实现」由依赖图工具跑出、「残留旧路径」取 plan-sprint 写的 `supersedes` 字段（非 develop 自列）。另**「复杂度负责人」这个角色与本仓模型冲突**（决策#1/#16 已废角色身份），须翻译成**绑 `architecture` discipline 授权的动作**（同「安全敏感改动须 architecture 裁决」的形状）；「复杂度结论：增加/不变/降低」这类自评栏建议直接不要，两期内必成人人填"不变"的仪式。**搁置理由**：同上条（冻结 + 等重建项目实跑）。**重启条件**：与上条同批评估；届时先看有没有真的独立枚举源可挂，挂不上则本条降级为"不做"。| 来源：2026-07-29 对重建计划的第三方审查意见（用户转述）
