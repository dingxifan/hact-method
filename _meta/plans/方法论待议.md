# 方法论待议

> 2026-09-16 审查收敛调整已获授权并在本仓落实：专项按真实改动触发、原独立审查员接续、建议不阻断、同快照补证仍计三轮。对应计划为 `2026-09-16-bounded-review/task_plan.md`。不重启下方扇出/profile、第四轮或 A 类主流程整体重构；旧待议论证保留。

> 2026-09-08 本轮状态覆盖：下方个人 notes 设计乙/O1 双源注入条目因强制私人积累链退役而关闭、不做；B 类“API/type 加法一律升级、只能 none”的旧结论由本轮局部兼容 governed 规则取代。仅覆盖这些直接相关项，其他主干待议不借本次调整实施。

- [ ] 2026-09-08 | **A 类主流程整体重构暂缓（用户裁决）**：普通 A 类是否从整期 PRD→TRD→全部任务包前置改为整体目标确认后滚动细化、逐段交付，先记录不实施。用户仍要评估其变化规模与风险；仅在用户明确重启后讨论 Gate/规格深度调整。当前另行授权管理减法和 B 类轻量化，不借此取消 A 类 G1/G2/G3 或改为边写边确定业务承诺。 | 来源：本次主/侧对话

> **审查范围修正（2026-09-06）**：本次按用户裁决落实可信开发方前提，直接取消对恶意/有意绕过的审查义务，见 `2026-09-06-review-trust-scope/`。下方三轮封顶条中的“范围过宽”已有这一类明确出口；不表示开放第四轮，不重启扇出/profile 裁剪等相邻改造。历史中的无界防绕过论证不作为当前执行要求。

> **状态覆盖（2026-09-04）**：下方 2026-08-03「串行塌 wave」旧条保留历史论证，但其“搁置/冻结”状态已失效；`single-operator-wave` 已落地，并在独立复审后补 per-task commit、整组恢复、有效 risk 前置与升档拆分。后续只观察真实团队并发样本，不再把它当未实施候选。

- [x] 2026-05-10 | **【hact-app 消费侧·三条合一，载体已停摆】**：原为三条独立条目（Gate 判断标准统一 / status.yml 写入校验 / status.yml 接续一致性核查），解锁条件同为「hact-app sync 切读 status.yml」。**2026-07-30 复核合并**：hact-app 最后一次提交 2026-06-03、仓内至今无 status.yml 文件——三条不是"还没到时候"，是挂在一个停摆的载体上，逐条复核纯属空转。**待做内容**：① Gate 判断改由 sync 解析 `gates.md` 直接驱动 iteration gate 字段（现靠 prd_path 等文档文件推断，与 CC 写的 gates.md 脱节）；② `status.yml` 写入校验——仓内固定 Node 脚本 `scripts/check-status.js` 做 **schema 校验**（枚举/date 正则/必填键/schema==1，非仅语法 parse，因 hact-app 对脏枚举是静默降级 `normalize()`、语义错比 parse 失败更隐蔽），分级触发（gate bool 翻转不校验，仅 `tasks[]`/`integration_tests[]`/`code_reviews[]` 等数组写入时校验 + 读回关键字段），纪律统一声明进 `skeleton/07` §五、`init-project` 铺脚本与 js-yaml 依赖；③ 接续一致性核查——`templates/CLAUDE.md` Step1 推断前对比 status.yml.gates ↔ gates.md、tasks ↔ sprint.md，不一致报告等人工确认。**解锁条件：hact-app 复工并切读 status.yml；复工前不再逐条复核。**| 来源：hact-app v3 feedback + 2026-06-16 method-adjustment（2.1 / 2.2）<br>合并前三条的完整原文见 git 历史（本行 2026-07-30 复核前的版本）<br>**2026-08-30 关闭·判为不做（载体已正式退役）**：用户确认 hact-app 已死，看板应用相关内容本轮已从方法论全面移除——`init-project` Step 5（注册 + webhook + 验证）整段删除、`_meta/hact-config.md` 的看板地址与 CC_TOKEN 删除、`skeleton/06/07` 与 guide 00/01/02/03 的看板叙述改写、BRIEF 阶段表中「开发看板应用」阶段取消。本条三项待做（Gate 判断由 sync 驱动 / status.yml 写入校验 / 接续一致性核查）的**消费侧不复存在**，故不是"押后"而是**不做**。其中真正有价值的那半——status.yml 的机械校验——已由 `check-sprint.js`（三方一致 + 第 9 项审计留痕完备性）与 `check-gate.js` 在本仓侧承担，无需另起。
- [ ] 2026-05-31 | 设计乙（个人 notes 即时生效扩展）：当前双源规范只在 draft-tech-design 生成端注入执行人 notes `[规范]`（设计甲），依赖"架构≈开发"高度重叠。团队分化（架构与开发分离）后，develop / pr-review 加载端是否也叠加加载本人 notes 以即时生效，待评估。| 来源：personal-notes-accumulation 方法论调整
- [ ] 2026-05-31 | checklist 双源对称（O1）：standards 已双源（生成端注入个人 `[规范]`），但 checklist 不由 draft-tech-design 生成，本人 `[checklist]` 当期不生效、只能经 harvest-notes 上提后下期全队用。是否在 develop 自检加"过一遍本人 notes `[checklist]`"，与设计乙同根，待团队分化一并评估。| 来源：personal-notes-accumulation 方法论调整
- [x] 2026-06-16 | 共享资产依赖完备性（团队期）：本轮已把 `depends_on` 补进任务包字段（清 P1 脱节），消费方靠反查。但"多任务并发改同一共享表/枚举/类型、漏标 depends_on 边"在团队期才真正咬人。待团队引入后，给 plan-sprint Step3.5 独审增第⑤类「共享资产依赖完备性」——交叉比对各任务包 `files`，≥2 任务碰同一共享文件/表/枚举则校验 depends_on 边标全（files 字段结构化，机械可查）。单人串行暂靠 Step2.5 交付判断 + 拾取顺序兜。| 来源：2026-06-16 method-adjustment（1.7）<br>**2026-09-04 已落地**：任务包新增 `asset-writes`，`check-sprint.js` 两两交叉 `files` 与共享资产键；同写且无任一方向依赖路径直接 FAIL，正反夹具覆盖同资产、同文件与已串行三种情形。
- [ ] 2026-06-16 | develop 选读 decisions.md（防重新发明被否决方案）：当前 decisions.md 是「决策/原因/日期」结构、无 files/模块映射键，develop 无法机械判定"哪条 decision 与我的 files 相关"，且 decisions.md 当前空表、单人阶段无积累否决决策，风险未实际发生。待 decisions.md 有实际积累 + 补"涉及模块"列后再议；非做不可时改由 plan-sprint（本就读 decisions.md）把相关否决决策摘要写进任务包 `do-not`，不让贫信息的 develop 侧去 files-match。| 来源：2026-06-16 method-adjustment（1.7 主张 B）
- [x] 2026-06-16 | pr-review 批量时机规则对串行依赖链的表达缺口：pr-review.md §会话启动写"同层全部任务推 PR 后批量审查"，但未区分**并行独立任务**（可真正批量）与**串行依赖链**（A→B→C 各为独立 PR，B 依赖 A 合并到 master）。后者无法等全部推完再批量——B 分支必须从 master 切，A 未合并则 B 的代码在 A 分支上，形成 stacked PR（被 develop.md 禁止）或分支污染。develop.md 已通过阻断条件（「独立 PR 未合并则依赖它的任务不可拾取」）在执行层强制了逐个合并，但 pr-review.md 批量时机描述与此隐含矛盾。建议在 pr-review.md §批量时机补注：「串行依赖链中的独立 PR（A 是 B 的 depends_on）须逐个 review 合并，不受同层批量等待限制；批量等待仅适用于互不依赖的并行独立任务。」| 来源：org-krm-v2 v5 backend 串行链（001→002→{003,004}→005）实操反馈
- [ ] 2026-06-16 | relevant-standards 覆盖度扶正为真机械判据：plan-sprint 字段保真自检目前对"relevant-standards 是否覆盖 files 所属强制规范"靠语义判断（移交 Step 3.5 独审），因 standards 模板无"文件类型→强制规范节"映射表，机械查不了。若团队引入后文件命名规范化（可按 pattern 归类），可在 `templates/standards/backend.md`/`frontend.md` 顶部各加一张"文件类型→规范节"映射表（如 `*.controller.ts → §响应格式+§入参验证`、`*.vue → §设计系统+§组件`），届时这条即可从独审语义判断升级为自检机械硬卡。单人阶段 ROI 低，暂不做。| 来源：2026-06-16 method-adjustment（1.3 字段自检升级保真，替代 B）
- [x] 2026-06-18 | 第①类「真·同步滞后」的 Step 0 改造：项目仓 `templates/CLAUDE.md` Step 0 的 `git -C ../hact-method pull 2>/dev/null || echo "已同步"` 会吞掉"本地不在 master / 凭据失败 / diverged"等错误、还输出"已是最新"的假成功信号。改：去 `2>/dev/null`、显式校验当前在 master 分支、"已同步 N 个 commit"声明改为基于 `git rev-parse HEAD` 前后对比的真实结果（拉到才说拉到）。与本轮 Gate 冷核（第②类）根因不同、文件不同，另起线。| 来源：2026-06-18 gate-criteria-cold-check 推演（第①类）<br>**2026-07-11 已落地**：Step 0 改为 `sync_repo` 函数——不吞 stderr、`pull --ff-only`（diverged/冲突/凭据失败原样报错）、hact-method 显式校验在 master、拉取数由 HEAD 前后 `rev-list --count` 实测；声明纪律补"失败不得改写为已同步"。项目仓不限分支（断点续做可能在任务分支）；harvest-notes 的 `2>/dev/null` 是 clone 兜底路径、不在此列。五情形 + 端到端已实测。
- [x] 2026-06-19 | 【全局方向】质量模型从"规范遵循验证"转向"输出可测试性"：当前方法论在规定输入侧投入最多（规范/冷核/Gate），在输出验证侧最薄（仅末端 integration-tests + manual-test）。方向性转变：每个阶段的产物应定义"如何自动验证正确性"，而非只定义"格式应该是什么"。具体落地路径待讨论，可能影响 Gate 机制重新定义、specs-structural 完成判据从清单改为可运行检查。完整分析见 `_meta/plans/2026-06-19-structural-review/findings.md` §四。| 来源：2026-06-19 结构性审查<br>**2026-07-30 关闭·已实质落地**：这条写的是方向、没写落地路径，而它已经被逐块实现了，只是没人回来划掉。落地物点名如下（下一轮不要再把它当未开工的大方向重提）：**地基三档**（构造上不可能 > 机械探测 > 人审，决策#25——即"产物如何被自动验证"的分级形式）+ **负向反例夹具纪律**（规则须有一份会被它拦红的代码，foundation-review G1「亲手撞过反例才算数」）+ **自绿须干净环境复现**（G2）+ **穿透流禁 fixture 抄近路**（GIT-API 改造）+ **视觉冒烟断言篡改自证**。file-extract v1 是它的完成形态：14 类负向反例全部按预期报红、断言有效性靠注入变异自证、**恒真式断言被当作缺陷抓出来**（doc-extract v6 前端 S15：弹窗关闭后元素仍在 DOM，按计数判的断言构造上无法侦测关闭 = 永远假绿）。原设想的"Gate 判据从清单改为可运行检查"也已由各 `check-*.js` + `check-gate.js` 兑现。
- [x] 2026-06-19 | develop 拆分为多个 task type：source=sprint/integration/manual-test/bug/optimization 五条路径上下文加载/反馈去向/checklist 完全不同，强行合并进一个471行规范。拆分后每个 type 的规范可大幅压缩，同时消灭"按属性分支在运行时路由"的解析错误风险，缓解 context 压力。影响：specs-structural/develop.md 拆为多份、specs-execution/develop.md 拆为多份、task type 枚举扩展。| 来源：2026-06-19 结构性审查 #1<br>**2026-07-30 关闭·不做**（两条独立理由，任一即足）：① **与决策#25② 正面冲突**——「develop 不拆、`source=foundation` 做成一味」是地基层设计时明确权衡过的结论（与 draft-foundation 不并入 draft-tech-design 的判断相反，理由是 develop 各 source 共享执行+独审+合并核心、只差进料口）；② **已有反向实证**——sub7 于 2026-06-19 真按本条拆过（471 行 → core+3 壳），**次日 2026-06-20 撤销复位**。留着这条只会让下一个人重推一遍已被否掉且已试错过的方案。context 压力那条动机另有解（决策#26 的活文档归档约定 + 降档）。
- [x] 2026-06-19 | B 类任务升级触发检测（"是否修改共享契约"）：当前 B 类分类标准是"来源（bug/optimization）"，真正边界是"是否修改 TRD/standards 等共享契约"，两个维度不等价。一个 B 类优化任务实际改了接口契约时，它绕过 G2/G3 保护、无任何机制检测，在真实项目中必然发生。在 dispatch-new 或 develop 入口增加"是否改动共享契约"的自检触发，命中则提示升级为 A 类 revise-doc 路径。| 来源：2026-06-19 结构性审查 #6<br>**2026-09-04 已落地**：`dispatch-new` 将已签文档与公共 schema/API/type/enum/event/state 的加法也列为硬升级；B 包固定 `contract-impact: none`，`check-b-task.js` 在派发、develop 认领与 pre-commit 三处核字段和高置信契约路径。
- [ ] 2026-06-19 | TRD shared-type-first 架构（解锁并行化前提）：TRD 按 API 模块并行生成是降低 context 压力的有效方向，但各模块共享数据模型（User/Dept/Task 等实体）。解法：在 API 模块生成前，先跑"共享类型定义" subagent 产出 schema.md，所有后续 API 生成 subagent 引用它。这是 generate→validate loop 在 TRD 阶段落地的前提。| 来源：2026-06-19 generate→validate loop 分析
- [ ] 2026-06-22 | 【前端规范一致性框架落地】：JHH-Nortion 审计暴露两个结构性失效（规范无防线 + 注册表不可信）。方向已讨论清楚，需在具体项目（JHH-Nortion 或 hact-app）落地以下五件事：① lint 规则（禁 hex 字面量、禁 `*Labels`/`*Options` 本地重声明）挂进 pre-commit-hook；② `scripts/check-reusables.js`（验登记路径存在 + 废弃资产无引用）；③ plan-sprint 任务包模板增加"复用决策"字段（UI 元素 → 用哪个现成组件 / 新建并登记）；④ plan-sprint Step 3.5 独立审查扩展复用决策 + 交互模式决策两个维度；⑤ TRD 接口设计段增加交互模式规格字段（防抖时长、loading 态、错误处理）。完整框架见 `_meta/plans/2026-06-22-frontend-consistency-framework/findings.md`。<br>**2026-08-03 ② 由"已写"升为"已生效"**：`check-reusables.js` 07-30 落地时只是文件存在——本轮实测它**在任何项目仓都没接进门卫**（四个装了 hook 的仓路由命中数全为 0，四个仓根本没装 hook），即"落地"到"生效"之间有 4 天真空且无人发现。现已随四仓门卫部署真正接上，并立刻由门卫自己报出那条真命中（`JHH-Nortion/reusables.md:19` 登记的 `StatusSelect` 全仓不存在）。**② 就此结清**（"废弃资产无引用"那半仍不做，需语义判断 + 全仓引用扫描很贵）。前端条剩余项不变。 | 来源：JHH-Nortion 组件复用与规范一致性审计 + 2026-06-22 方法论讨论<br>**2026-06-28 部分落地**（hact-app v8 视觉低级错误触发）：本框架缺的「视觉地基层」（跨切面公共件无人认领，R2/R3）+ token 落地门禁（R1，落 develop lint 层）+ 视觉冒烟断言（R4，前移到 generate-integration-tests）三件已落，见 `_meta/plans/2026-06-28-visual-baseline-package/`。**剩余仍待**：① 组件复用 lint（禁 `*Labels`/`*Options` 本地重声明）② `check-reusables.js` ③ 任务包「复用决策」字段 ④ Step 3.5 复用 + 交互模式维度 ⑤ TRD 交互模式规格字段。<br>**2026-07-30 复核·两个根因只解决了一个**：本条当初立于两个结构性失效——「规范无防线」+「**注册表不可信**」。前者已被**地基三档**解决（file-extract 实测：dep-cruiser + ESLint 构造级墙把「单例 api client 外禁 axios/fetch」「业务页只能是 AppLayout 的 children」「前端不得按 status 推导能做什么、白名单只开单一 mapper」拦成构造上不可能，配 14 类负向反例）。**后者原样未动**——逐项核实 file-extract：无 `*Labels`/`*Options` 类 lint（①），**全仓无任何 reusables 校验脚本**、`reusables.md` 零机器校验（②）。而两个月里读它的地方反而变多了：`draft-tech-design` / `plan-sprint` / 今天新加的 `draft-prd` as-built 核对都把它当权威源读（读的正是「标杆切片登记」）。**读它的地方越多，它不可信的代价越大——② 的优先级据此上调**（曾误判为"已被地基覆盖"，2026-07-30 核实后收窄）。<br>**2026-07-30 ② 的便宜一半已落地**：新增 `templates/scripts/check-reusables.js`（登记路径存在性 + 登记完整性；「废弃资产无引用」那半仍不做——需语义判断且全仓引用扫描很贵）。门卫两个触发口，第二个是主力：改了 `reusables.md` ∨ **本次 commit 有文件删除/改名**（失真的主要发生方式是资产被搬走而表没跟着动，此时表纹丝未动、只盯第一个口永远发现不了）。分级：仓内彻底搜不到 = `FAIL`；能搜到但不在登记位置 = `🧑`（多为按子目录相对登记的写法差异）；无「已落地资产」段的早期模板 = `🧑` 跳过、不拦死存量仓。**实测**：6 个真实项目仓扫一遍，**0 误报 1 真命中**——`JHH-Nortion` 登记的 `StatusSelect` 组件全仓不存在、也无任何引用（正是本条「注册表不可信」的原型）。<br>⚠️ **过程留痕（比结论更值得记）**：第一版按"路径相对仓根"严判，在 `JHH-Nortion` / `doc-extract` 上报 **41 条全红**，逐条核实**全是假阳性**——两仓分别按 `src/`、`apps/web/` 相对登记。一个 41 条全假阳性的检查器上线当天就会被 `--no-verify` 绕过。真值是靠"仓内后缀搜不到"这条更强的判据换来的。剩余 ①③④⑤ 仍待。<br>**2026-08-17 ② 的另一半（「废弃资产无引用」）方案已成型并实测，但按用户判断暂不落地——方案与证据原样记此，落地时不必重推**：<br>· **判据**：只扫 `components/` 下的 `.vue/.tsx/.jsx`，该组件名在全仓**代码**里一处引用都没有 → 报 🧑。views/pages 不扫（文件式路由下零引用是常态，全假阳性）；测试 / `test-fixtures/negative/**` / `.stories.` 排除；框架约定文件名（`page`/`layout`/`global-error`/`index`…）跳过。<br>· **原先不做的两条理由在收窄后都不成立**：组件必须显式 import，零引用是机械事实而非语义判断；扫描范围是组件目录而非全仓资产，700 文件的仓上秒级。**非组件资产（service/util/DTO）仍不做**，理由未变——经 DI、装饰器、glob 装载（TypeORM migration 即是），零引用不构成机械事实；后端若要覆盖须换判据（如"NestJS module 未 import 的 provider"），不是把同一条判据放宽路径。<br>· **要害是只认代码引用、不认文档提及**：残留组件的典型形态正是"代码里没人调、backlog 里挂着一条待清理"，把 `.md` 算作引用会漏掉**全部**真命中。<br>· **恒 🧑 不 FAIL**：动态 import / 字符串注册 / 框架约定会误报，拦死会让门卫被整体绕过。<br>· **门卫需加第三触发口**「staged 含 `components/` 下组件」——引用方被改到不再 import 是普通 modify、路由不到；但残留的主要发生方式是"新实现建起来、旧实现没人删"，那一刻新组件必在 staged 里，是唯一能当场撞见的时点。<br>· **实测（9 个真实仓 dry-run）**：**0 误报 1 真命中**。命中者 `mail-ai`：`ComposePane.vue`(766 行，被 `ComposeModal` 取代)、`common/MailboxToolbar.vue`(557 行，2026-06-24 建、06-25 改一次后再无引用——存活 1 天)、`common/EmailListToolbar.vue`(295 行，v4 TRD 规划的共用工具栏)，合计 **1,618 行 = 其前端组件总量(22 个/10,508 行)的 15.4%**，三者均未登记在 `reusables.md`、无配套 spec。`loxsonoa` 因 vendor 框架（`ruoyi-office-vben` 等）10,297 个代码文件超扫描预算被跳过（🧑 提示，非误报）。<br>· **最值得记的是 `ComposePane` 的形态**：`backlog.md:17` 2026-07-04 就记了"后续需单独判断是否仍有调用价值"、标优先级低，42 天后仍在——不是忘了，是**明确记账后永远排不上**。"只在 backlog 记一笔"不等于会被清理，这条本身就是证据。<br>· **驱动来源**：2026-08-12~15 全企业提交全量分析。副产的一处**自我更正**（避免下次重推）：4 天窗口测得删/增 0.045 是**取样偏差**——窗口内撞上两个 V0 立项（awuchi/likesales）+ file-extract 仅 17 天，73% 新增行落在全新文件。终身口径（仅生产源码、排除测试）实为 **mail-ai 0.142 / jhh-noriton 0.167**，源码文件历史删除率 4.5% / 3.6%。真正的问题**不是"整个模块废弃后留着"，而是组件级替代残留**。<br>· **暂不落地的理由（2026-08-17 用户判断）**：改动虽小，但要生效须分发到各项目仓并重装门卫，当前多项目并发在跑，动门卫有中断风险。**落地时的分发实况（已核）**：`check-reusables.js` 副本在 mail-ai / jhh-noriton / file-extract / likelicagent / likesales / awuchi 六仓存在且**与模板逐字节相同**（无副本→模板漂移，可直接覆盖）；document-query / rms-code / loxsonoa 三仓无此脚本；`pre-commit-hook.sh` tracked 脚本 7 仓有、rms-code 与 loxsonoa 无。**重启条件**：并发项目进入低峰，或与门卫分发（见本清单末「门卫部署不可复现」条）同批做。<br>· **配套产物**：`mail-ai` 清理任务包 `mail-b-025`（删三组件 + 结清 backlog:17 欠账，6 条 AC 含 build/test/脚本复验/pinchtab 三条冒烟，`supersedes` 4 条）当时只写在临时克隆里、**未推**，需要时按本条数据重新生成即可。
- [ ] 2026-06-22 | 【后端规范一致性框架落地】：与前端框架同源（同一 findings 文档），后端三维度为：规则层（响应格式/DTO 校验位置/错误抛出方式）→ TypeScript strict + ESLint；复用层（分页 helper/过滤器/通用 DTO/查询工具）→ reusables.md 后端区块 + check 脚本；模式层（软删除/事务边界/批量失败处理）→ TRD 接口设计段 + 任务包。核心与前端的两个差异：① 后端规则层机械化覆盖率更高（无视觉判断盲区）；② 后端多一个主力机制——AC → Given/When/Then → 可跑测试，直接验行为正确性而非规范遵循。需落地五件事：① TypeScript strict + ESLint 自定义规则（响应格式、DTO 校验、权限守卫）挂进 pre-commit-hook；② reusables.md 增后端区块 + check-reusables.js 覆盖后端路径；③ TRD 接口设计段增 Given/When/Then 规格字段；④ 任务包模板增测试要求字段（来自 AC 精化）；⑤ hact-method 补技术栈子模板层（`templates/standards/frontend-vue3.md`、`templates/standards/backend-nestjs.md` 等），当前模板只含结构提示。完整框架见 `_meta/plans/2026-06-22-frontend-consistency-framework/findings.md` §七-十。<br>**2026-08-03 ② 由"已写"升为"已生效"**：`check-reusables.js` 07-30 落地时只是文件存在——本轮实测它**在任何项目仓都没接进门卫**（四个装了 hook 的仓路由命中数全为 0，四个仓根本没装 hook），即"落地"到"生效"之间有 4 天真空且无人发现。现已随四仓门卫部署真正接上，并立刻由门卫自己报出那条真命中（`JHH-Nortion/reusables.md:19` 登记的 `StatusSelect` 全仓不存在）。**② 就此结清**（"废弃资产无引用"那半仍不做，需语义判断 + 全仓引用扫描很贵）。后端条剩余项不变。 | 来源：2026-06-22 方法论讨论（前后端规范一致性）<br>**2026-07-08 ⑤已落地**（决策#27 技术栈剥离）：栈子模板层建成（`frontend-vue3.md` / `backend-nestjs.md`，通用模板瘦身为栈无关原则），standards 播种双源升三源（通用 + 栈子模板 + 个人 notes），skeleton/specs 去栈措辞。①-④ 仍待。<br>**2026-07-30 复核**：②（`check-reusables.js` 覆盖后端路径）与前端条同命——file-extract 全仓零 reusables 校验，见上条「注册表不可信原样未动」。①（响应格式/DTO 校验/权限守卫的 ESLint 强制）在 file-extract 是**部分兑现**但走的是另一条路：架构墙管的是**依赖方向与边界**（`api-no-direct-orm`、包边界、幽灵依赖、条件导出键序），不是响应形状与 DTO 校验位置——后两者仍靠 standards 文本 + 独审。③④ 未动。
- [ ] 2026-06-22 | 【自动化水平提升·GUI 笨宏换窗续设计】：L3 编排层（站到站推进、换窗续跑）仍全人工。已讨论清楚的方向：本地确定性宏（AutoHotkey/pyautogui）监听 flag 文件 → 开新窗敲恢复词，绕开 Credit Pool 保留 Max 订阅。待续设计的命门：**CC-1 换窗前必须往交接棒文件写哪些东西**，才能让 CC-2 读文件后完全自恢复不丢线。附带已拍板的两个决定：① G2/G3 纯技术判断可并入中段无人区；② manual-test 移出 G4，作为 owner 意图闸与 G1/G5 并列。完整讨论见内存 `project_automation_gui_handoff.md`。| 来源：2026-06-22 `/remote-control` 闲聊
- [ ] 2026-07-08 | checklist 栈子模板对称拆分：决策#27 已把 standards 拆成「通用 + 栈子模板」双层，但 `templates/checklists/frontend-checklist.md` 仍内嵌栈特定项（vue-tsc / Element Plus 模板禁忌 / stylelint 规则名）。当前靠 checklist 自带的"诚实前提"（项目未配对应规则的项落留人走查）兜底，拆分收益低，暂不做。待栈子模板经真实项目验证 + 出现第二个栈的项目时，按同一模式拆 `frontend-checklist-{栈}.md`。| 来源：2026-07-08 技术栈剥离（决策#27 遗留项）<br>**2026-07-30 条件改写**：字面上的「第二个栈」**已经存在**（`loxson-salary-new` 是 Java/MyBatis），但它不由本仓 standards/checklist 驱动，拆了没有消费方、收益不兑现。条件收窄为「**本仓驱动的**第二栈项目」——即由 `init-project` 立项、`draft-foundation` 确立技术层、按三源播种 standards 的项目里出现非 TS 栈。在此之前维持不做。
- [ ] 2026-07-10 | hotfix 前端设计门减重（⚖️ 化）：B 类 hotfix 若**无新增视觉/交互形态**（只改已有 UI 的业务逻辑/数据限制，不新增组件/页面/交互模式），前端设计覆盖检查从人工门降为 ⚖️ 默认判定——CC 自判"无新增视觉形态"并输出结论+理由后直接续跑，可推翻。符合决策#28 分级模式，与运行时无关（CC/Codex 同受益）。受 2026-07-08 机制冻结约束，暂不落正文；冻结解除或下次被打疼时按 develop.md 前端设计覆盖检查处加一个 ⚖️ 分支落地。| 来源：2026-07-10 JHH-Nortion jhh-b-004 Codex 试点反馈（活动挂接父级互斥修复，纯业务限制、无新视觉形态，设计门体感偏重）
- [x] 2026-07-11 | pre-commit 门卫对 init 提交误报（check-sprint 路由过宽）：init-project 首次 commit staged 了 `iterations/v1/queue/done/.gitkeep`，`pre-commit-hook.sh` 据路径路由到 `check-sprint.js`，报"queue 目录无任务包"拦截——而立项时 queue 必然为空（任务包到 plan-sprint 才产生），任何新项目 init 提交都会被拦，只能 `--no-verify` 绕过。修法候选：门卫路由排除 `.gitkeep`，或 check-sprint 对"queue 仅含 .gitkeep"视为初始态跳过。受 2026-07-08 机制冻结约束暂不改模板，解冻后落地。<br>**2026-08-03 关闭·主体早已落地，条目本身过期**：正文写「受 2026-07-08 机制冻结约束暂不改模板，解冻后落地」，但**模板早就改了**——`templates/scripts/pre-commit-hook.sh:68-69` 已是「只认 `.md`：init 提交只 stage `queue/**/.gitkeep`，不触发本检查」。即修法候选①已实施，条目未及回来划掉。**残留的那半（存量项目仓的 hook 复制件仍是未修版）本轮已处理**：`file-extract` / `doc-extract` / `JHH-Nortion` / `mail-ai` 四仓已重装模板 hook 并做真触发测试；其余四个存量仓用户裁定已结束、不再维护。**但残留背后的普遍问题另立了新条**——hook 住在 `.git/hooks/` 不进版本控制，本轮部署只是本机状态，见 2026-08-03「门卫部署不可复现」条。 | 来源：2026-07-11 doc-extract init-project 实操<br>**2026-07-11 已落地**（用户点名修 bug，属既有机制纠错、非新增机制，不违冻结）：取路由方案——`pre-commit-hook.sh` 的 check-sprint 触发从 `queue/` 收窄为 `(sprint\.md|queue/.*\.md)$`，`.gitkeep` 不再触发；check-sprint.js 本身保持严格（显式调用时 queue 空仍 FAIL，G3 判据不松）。init 提交放行 + 真任务包提交仍拦截已实测。注意：已建项目仓（doc-extract 等）的 `.git/hooks/pre-commit` 是复制件，需手动重拷一次模板。<br>**2026-07-30 复核·残留仍在且比原文写的更宽**：原文把触发场景写成"任何新项目 init 提交"，实际**每次新建迭代都会触发**——`draft-prd-vN` 为 vN+1 建 `iterations/vN+1/queue/done/.gitkeep` 时，与 init 提交同形（staged 只有 `.gitkeep`、queue 无任务包），旧版 hook 照样拦。**实测存量仓 hook 状态**（2026-07-30 扫本机各项目仓）：`mail-ai` / `JHH-Nortion` / `doc-extract` **仍是旧版**（会误报，前两者是在用的试验田、下次开新迭代即撞）；`file-extract` 是已修版（2026-07-29 立项，取的是修好的模板）；`org-krm-v2` / `wms-bellwether` / `loxson-salary-new` / `hact-app` 无 hook。**待办改形状（不是常备杂活，是复工触发项）**：<br>① `doc-extract` **划掉**——冻结在 v6、不会再有 v7，那个 hook 永远不会再被触发，零价值。<br>② `mail-ai`（末次提交 2026-07-07）/ `JHH-Nortion`（2026-07-10）**均已闲置、重心已移到 file-extract**，价值完全取决于会不会复工；不复工即零，复工也只是新建迭代那一次 commit 被拦（`--no-verify` 可过）。真正的代价不是那两分钟，是**养成"门卫误报、绕过就行"的反射**——同一个 hook 上还挂着真检查。<br>③ **且重拷不是无风险操作**：两仓 hook 落后模板的地方不止 `.gitkeep` 判据（mail-ai 少整段、JHH 的 G4/G5 判据也旧），而**当前模板 hook 会调 `check-ux.js`，两仓 `scripts/` 里都没有该文件**——直接覆盖可能把一个误报换成一个报错。正确做法是更新仓内版本化的 `scripts/pre-commit-hook.sh` + 重装 + 核对脚本集齐全，每仓十几分钟。<br>**故收窄为**：`mail-ai` / `JHH-Nortion` **若复工，开工第一件事是同步 hook 与 `scripts/`**。挂进复工检查、不占常备待办带宽——需要的那一刻自己触发，不需要则永远不必做。
- [ ] 2026-07-12 | G3·机械级是否要求"接进自动门"：foundation-review 认证机械级时，检查只要在配置里存在（`.stylelintrc.json`/`eslint.config.mjs` 有规则）算达标，还是必须接进一道**真在自动跑、能拦坏代码进 master 的门**（CI/pre-commit）？doc-extract E5 实例：stylelint 规则在配置里但仓库无 CI，红线"只活在本地手敲命令"，图省事的人不跑 + 无 CI → 违规照绿进 master，实际滑回人审级。**本轮搁置理由**：① G1+G2 已吸收大半（规则有洞→G1 撞、检查没跑→G2 自绿复现暴露）；② 本方法拦 master 的门本就是 develop 自绿那一步、非 CI 服务器；③ 纯残余="必须另有常驻 CI"=新增基建要求，撞 2026-07-08 冻结。**落地候选**：Gitee Go 流水线 / pre-commit 钩子（本仓 `check-sprint.js` 门卫已有先例）/ develop 自绿即算。**重启条件**：价值随第五阶段真人 push 上升——真人 commit 不走 develop 自绿，那时 CI 才承重；与决策#24"团队引入期若需恢复独立人工 merge 门可再评估"同一时点一并重评。| 来源：2026-07-12 doc-extract V0 二轮独审 E5 + foundation-review 证据化收紧讨论（`_meta/plans/2026-07-12-foundation-review-probe-tightening/`）
- [ ] 2026-07-12 | V0 存在时 prep 阶段缺 as-built 对账（TRD/plan-sprint 照理想模型切包 ≠ V0 实况）：doc-extract V1 `de-v1-004` 执行 subagent 直接 blocked、0 代码落盘、浮 3 处设计裁决 + 另起 `de-rd-001` 对账任务——吃掉 develop ~27% 墙钟并拖尾 005/006/007（"对账族"）。根因=TRD 状态词汇/枚举（`extract_failed`、进行时分词）+ 抽取队列 + pipeline_transition 边 `enabled` 清单 与 V0 实际库（`status.yml`/`foundation.md`/迁移）不符，`draft-tech-design`/`plan-sprint` 未对账 as-built 地基。**是唯一漏网的独立病因类别**：B 类 P1–P5（修"声明档>实测档"强度洞）在本轮 develop **前**已全合、foundation-review 探针收紧也已落，**两道防线都盖不住它**——它不是"墙不够硬"，是"V0 按走骨架设计只建半条管线（一条边/一个队列/无 PDF 库）+ TRD 没对账实况"。时间线钉死其家=`draft-tech-design`：B 类 07-11 19:22 全合 → TRD/G2 21:02（在其后）→ TRD **仍**带错的状态词汇，即 as-built 早在位、是写 TRD 时没读实际库枚举（`plan-sprint` 忠实照 TRD 切包、非其错）。**落点**：`draft-tech-design` V0 存在时增一步"TRD 状态词汇/枚举/队列/pipeline_transition 边 对账 as-built 地基（读 status.yml/foundation.md/迁移）"。**搁置理由**：doc-extract 是首个走完 V0→V1 的项目，单样本不足立机制（守 2026-07-08 冻结 + empirical-harvest measure-first；记录≠部署）。**重启条件**：第二个 V0→V1 项目复现，或下个项目 `draft-tech-design` over 存量 V0 时顺势落。| 来源：2026-07-12 doc-extract V1 develop 耗时归因（de-v1-004 阻塞 escape-hatch，墙钟 7.3h 中最大单点）<br>**2026-07-28 重挂**：doc-extract 转入重建、不再迭代，原条件改由**重建后的新项目**承接（它会完整走一遍 V0→V1）。同时本条在 doc-extract 拿到了比"状态词汇对不上"更强的形态——同一 V0 关注点跑到 v6，`pipeline_transition` 累积 17 条边含三组环，"看似顺序生命周期实为可回退图"，无人负责复核整体是否仍自洽。落点因此扩为两半：① 原有的 TRD↔as-built 对账 ② 已登记地基关注点的**自洽复核**（设计稿 `2026-07-28-foundation-concern-coherence/`）。<br>**2026-07-30 复核·① 半收口，只剩 ②**：第二个 V0→V1 项目（file-extract）**复现了，但这次是被抓住的**——`fe-rd-001`：PRD 把模板版本承载定为「仓内 manifest 文件 + 文件适配器」，而 V0 的标杆穿透切片建的正是这条线（`template_version` 表 + CAS 列 + 契约冻结触发器 + 前端标杆页，均登记 `reusables.md`），`draft-tech-design` 疑点核实撞上它、判为「同一实体两个家」，走 `revise-doc(target=prd)` 由用户裁决保留 V0 实现。**说明 TRD 侧那道人工核实实际有效，真正的漏是 PRD 先把实体安错了家**——故本轮把 as-built 核对**前移到 `draft-prd-vN`**（commit `a19bd3d`），① 半收口，不再另在 draft-tech-design 加步。**剩 ②**：file-extract 只跑了一期、地基关注点尚未累积，验不了；挂 v2/v3 之后。
- [x] 2026-07-12 | GIT（generate-integration-tests）价值随 AC 链+独审前移而"挪位"、现瞄错靶：上游（行为化 AC 链 + per-task 对抗独审跑真库全量+反例）已清"装配整体跑通吗"，幸存 bug 只剩三缝——(a) 跨模块接缝 (b) 真实外部边界 (c) 真浏览器/真数据渲染。现状 GIT 测**安全中段 + mock 外部**、三缝都没碰 → 不仅低命中（harvest 2/118 + doc-extract v1 后端 0/31），更是**低信息量**（全绿≈无信息：测的地方本就不会错，全绿不能证 seam/boundary 成立）。**两半不对称**：① **GIT-API = 瞄错靶、可回收**——改成"一条真**全管线驱动穿透**（本项目会当场撞 de-v1-004 类'边 enabled=false/状态词汇'）+ **真外部边界冒烟**"，信息量即回（= empirical-harvest 已挂的改造候选）；② **GIT-web = 与必留的 manual-test（owner 闸、砍不掉）高度重叠**——v1 因环境无浏览器/pinchtab 直接降级并入 manual-test（静态 token 锚点核对 + develop 单测兜底，非静默通过、决策#28 ⚖️ 正确姿势），首次发现被 manual-test 支配，残余仅"自动回归 + 真数据形态"（harvest 浏览器回归逃逸 ≈0）。**证据强度不等**：GIT-API 低价值=2 点实证(2/118+0/31)+信息量论证，**较硬**；GIT-web 被 manual-test 支配=理论+harvest 视觉后移，v1 那次系**环境强制降级、非 by-design 对照**，属**弱支撑，需一次 browser-capable 环境真跑才够判**（另：若真实开发机也常跑不了 pinchtab，可用性低再记一记）。**落点**：`generate-integration-tests` 改造——API 转"穿透+边界"、web 侧正式化"并入 manual-test + 静态锚点代偿"为合法路径而非降级。**搁置理由**：GIT-web 欠一次真跑；守 measure-first + 2026-07-08 冻结（改造既有机制非新增）。**GIT-API 改造已 land**（2026-07-12，改 `specs-execution/generate-integration-tests.md` Step2 穿透替矩阵 / Step3 按流 / Step4 ⚖️ 双路由 / 新增 Step4B 边界闸 + `specs-structural` 字段规范·完成判据）：穿透替矩阵·数量按终态原则·失败 ⚖️ 双路由；边界闸=桩点即边界 ⚖️ 探测 + 🚫 真调 + 临时样本 + 非静默降级。设计稿 `_meta/plans/2026-07-12-git-api-reshape/design.md`。**下次 GIT 运行即改造自身第一手验证**（穿透是否比矩阵多抓 / 边界闸是否真拦外部接线）。**GIT-web 仍待**（欠一次 browser-capable 环境真跑再定要不要瘦身，web 侧本轮不改）。<br>**2026-07-28 取数窗口已开**：doc-extract v6 联调在会话内自备浏览器（playwright-core + Chromium，不入仓）跑通真登录与四条新路由，**这就是本条欠的那次 browser-capable 真跑**——v6 联调结束即收割：前端档实际拦下几条、与 manual-test 重叠多少。窗口一次性（doc-extract 此后不再迭代），错过则本条只能改挂重建后的新项目。<br>**2026-07-30 关闭·窗口已用、两半都有数据**（详见 `_meta/plans/2026-07-30-post-v1-parked-review/findings.md`）：**GIT-web** 那次真跑发生了——doc-extract v6 联调（`integration-tests/result-2026-07-29.md`）自备 playwright-core + Chromium 跑**完整档**：15 场景全 PASS、视觉冒烟 20/20、**零产品缺陷首发**。结论**两个方向都不支持**：不能砍（v6 自陈：v1–v4 连续四轮降级正是 v4 两个纯前端可达性缺陷活到 manual-test 末端的**机制性原因**），也不高命中。它真正的角色是第三种——**可视区 AC 的机械回归载体**（AC-19 预览只读 / AC-47 双标注，后端结构上覆盖不了）+ **视觉锚点回归** + **测试自身可信度的检验场**（S15 那条「弹窗关闭后元素仍在 DOM、按计数判 = 永远假绿」的恒真式断言，只有真跑起来才暴露）。**GIT-API** 改造的第一手验证见下一条（穿透流条目）。**残留已落**（2026-07-30 同日）：`generate-integration-tests.md` Step 4.5 增「本层的职责」段（可视区 AC 机械回归 / 视觉锚点回归 / 断言自身可信度的检验，并写明跳过它的代价是可视区缺陷改由末端人工验收兜）+ 前端场景生成增**禁恒真式断言**规则（禁用元素计数判弹窗关闭——组件库普遍关闭后保留 DOM 节点；一律按可见性判；每条断言自问"被测行为反过来时会不会红"）。| 来源：2026-07-12 doc-extract v1 GIT 结果（后端 31/31 全绿 0 命中 / 前端环境降级并入 manual-test）+ 理论分析，承 empirical-harvest generate-integration-tests 改造候选
- [ ] 2026-06-18 | Gate 冷核硬化（B3 hook + Layer A 上游防）：本轮只落地 Gate 完成判据冷核**软版**（规范步骤 + 凭证 + 人工抽看，无机械防线）。后续增强：(B3) `PreToolUse` hook 守签字动作，凭证缺失/过期/产物指纹不符则拦截签字（一个 hook `G{N}` 参数化包全 5 Gate，由 init-project 铺进项目仓）；(A1) 完成判据字段→产物模板空槽（PRD/TRD 预印 `入口:`/`draft-ux:` 槽位，空着即刺眼）；(A2) 机械 linter（Node，字段缺失/空段确定性报错，可与待议 `check-status.js` 合并）。软版扛不住"主会话整步跳过"和"把 FAIL 洗成 pass（六种伪造）"，硬化才补机械闸。完整设计见 `_meta/plans/2026-06-18-gate-criteria-cold-check/design.md` §二/§三。| 来源：2026-06-18 gate-criteria-cold-check（B3 + Layer A 延后）
- [x] 2026-07-21 | GIT 穿透流实际命中率待第一手验证（B2「新增全局接缝审」是否必要的前置）：doc-extract v5 二次全面独审出 8 阻断，报告据此主张在「develop 全 merged 之后」新增一道全局接缝审步骤——但核实 `iterations/v5/gates.md` G4 未签、`integration-tests/` 无 `scripts-v5.md`，**v5 根本没跑 `generate-integration-tests`**，而 GIT-API 已于 2026-07-12 改造成"穿透 + 边界"、位置正是该槽位。逐条比对 8 阻断：B3（改判端点窗口为零——报告自陈 happy-path 靠夹具把记录留在 `registered` 才绿，正是穿透流红线「禁 fixture 抄近路」所禁）、B1（提单类型三条实操路径证伪）**穿透流按现有判据应当撞上**；其余 6 条（TOCTOU 并发 / 双渲染 / mergeChunkedFields / 前端三条）穿透流看不见。**结论：不是缺一步，是已有的那一步没跑 + 进料口可能偏窄。** 落点候选：扩 GIT 穿透流进料口（如"上期能力回归"一条流），远比新增一步便宜。**重启条件**：v5 补跑 GIT 后，比对穿透流实际拦下几条 vs 二次独审的 8 条，用真实数据定。承 2026-07-12 GIT 改造条目「下次 GIT 运行即改造自身第一手验证」。<br>**2026-07-28 重挂**：v5 不会补跑（doc-extract 转入重建、不再迭代）；改挂 **v6 的 GIT 运行**——9 条后端穿透流已生成待跑，跑完即比对"穿透流实际拦下几条 vs v6 全面独审的 finding 集"。同为一次性窗口，与上一条同批收割。<br>**2026-07-30 关闭·两轮数据到手，且问题问错了**（详见 `_meta/plans/2026-07-30-post-v1-parked-review/findings.md` §三）：doc-extract v6 跑了 9 条穿透流、file-extract v1 跑了 6 条，产出形状**完全一致**——两个项目、两个栈、两个阶段，**code bug 各 0 条，抓到的全是「意图 ≠ as-built」**：v6 抓 1 条 `[阻断]`（`batch_no` 恒为 null，TRD 四处把它定为源文件的人类可读标识，实测 312 行里 298 行为空），file-extract v1 抓 **9 处** TRD↔as-built 不一致（退出码 1 vs 2 / 未写的存档范围校验 / stdout 与 `--out` 刻意不对称 / 错误码 CLI 路径不可达…）。**结论：穿透流是契约漂移检测器，不是缺陷网**——按"抓 bug"的口径衡量它一直显得低命中，是量错了指标。由此本条原提议（在 develop 全 merged 后新增一道全局接缝审）**不成立**：v5 那 8 条里穿透流本就该撞上的两条，成因是 v5 根本没跑 GIT；剩下 6 条（TOCTOU 并发 / 双渲染 / 前端三条）穿透流看不见，而"全局接缝审"同样看不见——它们的载体是独审与前端档。**残留已落**（2026-07-30 同日）：`generate-integration-tests.md` Step 2 增「产出预期」段（主类型是契约漂移非实现 bug；全绿 0 阻断不等于本层无价值、不构成该砍的证据）+ Step 4 增**「流全绿也要做契约对账」**块（全绿只证明实现自洽、不证明与 TRD 一致，漂移恰恰躲在全绿轮里；逐处对照接口形状/退出码/错误码/响应体字段，不一致记 backlog 待 `revise-doc(target=trd)`、**不改实现**）。另补一道可达性闸——该块所在的 Step 4 标题是"处理后端失败"、零失败时整步会被跳过，故 Step 6 与 `specs-structural` 完成判据各加一条勾选项。| 来源：2026-07-21 doc-extract v4/v5 经验总结核对（`_meta/plans/2026-07-21-scope-vacuum-and-feedback-routing/design.md` §一）
- [x] 2026-07-21 | 地基反转类迭代的「旧假设回查包」（报告 B1，真·新增机制、守冻结）：v4（一文件=一单证 → 拆多段）与 v5（类型是代码 → 类型是配置）都是**推翻早期全局假设**的迭代，两期最大残留都源于"全仓仍有代码依赖旧假设、但没有任何包被指派去找它们"。提议：这类迭代强制出一个不写新功能的回查包（任务=全仓找依赖旧假设的残留）。**搁置理由**：新增 task 形态、撞 2026-07-08 冻结；且与本轮已落地的 A2 归属真空检查部分重叠（后者查"包与包之间"，前者查"包与历史之间"，先看 A2 跑一期能拦多少）。**重启条件**：下一个地基反转类迭代先**手工做一次不入规范**，跑完看产出再决定是否立机制。<br>**2026-08-03 评估 → 关闭·不做（重启条件已满足，手工那次跑完了，结论是它的活已有人干）**：条件是「file-extract v2 地基反转先手工做一次不入规范，看产出再定」。**v2 确是地基反转**（`document-engine` 真落地、`apps/api` 首次真调 `screen`/`extract`、V0 的 `API_WIRED_PACKAGES` 占位边逐条移除）。但那次手工盘点**没有长成一个独立动作，而是被两条既有链整个吸收**：① `supersedes` 退役账——`fe-v2-009` 逐条核占位边，实测移除后该方向依赖边由 10 条涨到 18 条、**未从模块图消失**（正是欠账 #26 原文点名的陷阱，"未踩"是核出来的不是猜的）；② `backlog [欠账]` 的解除条件——两个半边（`validation-core` / 抽取基线取值）继续留账，各带可机读的解除条件，其中后者已于 v2 结清。**判据**：新增一个 task 形态的价值 = 它能捞到前两条链捞不到的东西；v2 一个这样的实例都没产出。**故不做**——其实质已沉淀为一条纪律并在 `backlog.md` 兑现两次：「退役声明须**逐个实体**核过还有没有调用方再写，不能按包整体判」。**保留重开口**：样本 n=1，且 file-extract 是**空库新建 + 单人**；若第五阶段出现「多人 + 真数据迁移」的地基反转，本条可原样重开。 | 来源：2026-07-21 doc-extract v4/v5 经验总结（报告 B 类 B1）<br>**2026-07-28 重挂**：改挂 doc-extract **重建后的新项目**——整个重建即一次最大规模的地基反转（重定义状态模型与编排契约、剥离双引擎），其阶段 0/1「盘点旧实现哪些依赖旧假设」正是本条要手工做的那一次。新项目跑完看产出再决定是否立机制。<br>**2026-07-30 重挂 file-extract v2**：v1 跑完了，但**它不是本条要的那种迭代**——file-extract 是**空库新建**（不迁任何历史业务数据），其"旧假设盘点"表现为跨项目搬运，已由 `fe-v1-008` 的 `conflicts.json` 承接（14 条冲突记录即那次盘点，见 #30）。真正的地基反转在 **v2**：文档引擎落地、`apps/api` 首次真调 `document-engine` 的 `screen`/`extract`，届时要真剥离旧引擎、且 v1 留下的 `API_WIRED_PACKAGES` 占位边要逐条移除并核对依赖边条数（已在 file-extract backlog）。**重启条件不变：先手工做一次不入规范，看产出再定。**
- [x] 2026-07-21 | AC 可达性自检 / 后端验收锚 / 豁免双侧实证（报告 B3-B5，checklist 类）：① **AC 写作自检**——凡 AC 出现容器/列表/状态名（"在复核队列可见""处于可重抽态"），须回答"该记录此刻在什么状态？到目标位置有没有边？"（v4 AC-02 + v5 AC-06/AC-30 三次复现，v5 AC-30 实测状态窗口为零）；② **后端任务验收锚**改为"前端能用它做成事"而非"我实现了 AC"（v4 frontend 连撞 3 处后端契约缺口 / v5 前端下拉列出提单而后端常量白名单拒它）；③ **豁免文本双侧实证**——凡"仅在 X 成立 / 仅 Y 不成立"类边界断言，独审须验两侧（v5 B2 的豁免把双渲染认证为合规路径；一条写错边界的豁免比未发现的缺陷更有害，它让人主动停止寻找）。**搁置理由**：三条都是 checklist 类增补，需等 A1 的载体（backlog `[欠账]` 链路）跑过一期确认不再被倒掉，否则写进去也是下次 wrap-up 被清。| 来源：2026-07-21 doc-extract v4/v5 经验总结（报告 B 类 B3/B4/B5）<br>**2026-07-30 落地·前置已兑现**（backlog `[欠账]` 载体跑过一整期：10 条逐条交定夺、全判"留下期"、条目留存未被倒掉）。三条各落一处，均为既有检查加判据、非新增机制：<br>① **AC 可达性**——`draft-prd-vN` 写完主体 AC 后与「功能类型触发检查」一并过（AC 出现容器/列表/状态名 → 追问"该记录此刻什么状态？到那个位置有没有一条边？"），并进 `prd-review` 第 2 类「AC 可验性」作为须单独过一遍的子项。命门写明：**这类 AC 形式上完全合格**（可验、有例子），linter 与独审都放行，只有问可达性才现形。<br>② **后端验收锚**——落 `templates/queue/task-package.md` 的 `acceptance-criteria` 字段注释（写包时可见）：锚在**调用方能用它做成事**，不是"我实现了 AC"；常见缺口=字段齐了但列表/白名单不放行、状态对了却没有取它的读接口。锚在自己这侧的话，缺口要等前端撞上来才出现。<br>③ **豁免双侧实证**——落 `develop-review` 新增【通用纪律】段（位置在【默认假设】后、【逐类检查】前，覆盖全部八类而非挂某一类）：边界断言两侧各撞一次，只撞一侧等于没撞（它在两种世界里都绿）；两侧验不了判阻断。理由写明**写错边界的豁免比未发现的缺陷更有害**——后者只是没被找到，前者让后面每个人在那里主动停止寻找。
- [x] 2026-07-29 | 矛盾当场闸 + 删除授权（退役账缺的另两半）：2026-07-28 落地的**退役账**（任务包 `supersedes` → develop PR 逐条结账 → wrap-up 签 G5 前核对）只解决了「记账义务」，漏两处——① **事后 vs 当场**：退役账在 PR/G5 才结，而复杂度是在「实现与黄金样本/已签契约冲突」的那一刻长出来的；现有 `escalate-if` / escape-hatch 管的是"上下文不足以决策"，**从没有一条规则说「兼容层 / 双路径 / 临时状态 / 兜底分支」本身就是失败模式**。拟落：冲突时**立即停止**、禁止用「两个都保留」收场，出冲突报告（冲突双方 / 实际调用证据 / 黄金样本影响 / 建议保留 / 建议删除 / **不裁决时会新增什么复杂度**）交人裁决——最后一栏是强制函数，逼着在决策当下把省事那条路的代价写出来（doc-extract 源码 130 处「回落」，每一处当时都便宜，没有任何一步要求算这笔账）。② **有义务没权限**：agent 发现死代码只能申报、无授权动手，于是进 backlog 后没有然后。拟落授权边界：授权范围＝**工作副本中与已确认契约冲突的旧实现**（不授权删生产库 / 原仓）；删前须引用扫描 + 黄金样本验证 + 影响说明；人裁决「保留 A 删 B」后**同一任务内**删 B 及其测试/适配器/死分支，不许拖期；**禁止改名 `legacy`/`fallback`/`compat` 后继续保留**（本仓判据现为「路由为零但仍注册 = 保留不是下线」，说的是同一件事但没堵改名这个逃生口——doc-extract 旧 `QwenEngine` 正是靠"休眠退役"四字活下来）；临时兼容须经批准并记负责人 + 删除条件 + 最晚删除阶段，否则默认数量为零。**搁置理由**：守 2026-07-08 冻结；且重建项目将先按此手工跑一遍（同 #28「旧假设回查包」的姿势：先手工做一次不入规范）。**重启条件**：重建项目跑完，看冲突报告实际产出几份、删除授权实际用掉几次，再定是否升成规范。<br>**2026-08-03 评估 → 关闭·两半都不做（病没复现）**：条件是「重建项目跑完，看冲突报告实际产出几份、删除授权实际用掉几次」。**① 矛盾当场闸**：v1 手工跑出的产物是 `packages/test-fixtures/templates/invoice.conflicts.json` —— **已经是机读结构**（`id` / `typeCode` / `subject` / `sides[]` 含"旧配置的解释"两侧 / 裁决），14 条，形态正是本条设想的冲突报告。而本条真正的主张是「**禁止用『两个都保留』收场**」——v2 的三条非空 `supersedes` 全部结论为**已下线**且各有 diff 级证据，一条兼容层都没长出来，**这个病没复现**。**② 有义务没权限**（agent 发现死代码只能申报、进 backlog 后没有然后）：同样证伪——wrap-up v2 三条 `supersedes` 逐条结账、该删的删了，`backlog.md` 20 开 16 关且半边闭合都带解除条件，不是"申报后没有然后"的形态。**故整条关闭**。**唯一保留的东西**是那条已生效的纪律（逐实体核调用方），以及本条原文里那个仍然对的判断：**「路由为零但仍注册 = 保留不是下线」**，它已经写进 `develop.md` §退役账判据。**重开条件**：出现第一个"两个都保留"收场的真实实例（`supersedes` 结论出现"保留"而理由是"暂时兼容"、无解除条件）。 | 来源：2026-07-29 对重建计划的第三方审查意见（用户转述）+ 2026-07-28 doc-extract v1–v6 代码审查（增生无退役）
- [ ] 2026-07-30 | **首次 harvest-notes 收割专项（317 条待收）+ 一个更大的发现：团队已在自发使用方法论，而状态文档不知道**：2026-07-30 起过一次 harvest-notes，走到会话启动 🚫 门即按用户指示停住（**一条未上提、游标未动、成员仓只读未碰**），改开专项处理。**待收清单**（游标表全部"尚未收割"＝有史以来第一次收割，全量在范围内）：axiaoke 153（规范 60/checklist 66/方法论 27）、dingxifan 111（29/36/46）、WayneLu 24（11/3/10）、ba-xiansheng 17（3/8/6）、winniemw 9（6/1/2）、lu-ailu 3（1/1/1）＝**合计 317 条**（规范 110 / checklist 115 / 方法论 92）；另 `[心得]` 31 条按红线永不收割；`dingxifan_admin`/`colin_zheng`/`xiaobainote`/`zhang_cheng_1377` 四仓仅含 init 播种的 README、无条目。**真正要讨论的不是收割本身**：① BRIEF/STATUS 写「第五阶段·团队引入未开始」，但 6 名成员在真记、WayneLu 与 dingxifan 07-30 当天仍在提交、**axiaoke 在跑一个本方法论仓从未见过的项目 `likelicagent`**（其 notes 里有 v3/v4 wrap-up 分流、lla-b-013/025/026 就地分流）——即存在**第二批真实使用者且未经引导**，含金量与风险都高于受控试验田，须先核实他们实际走到了哪一步（够不够第五阶段的完成判据「至少一名开发者独立完成一个 task 全流程」），**核实前不要改 STATUS 的阶段结论**；② 92 条方法论 vs 现有待议 31 条，原样追加会把清单撑成 123 条流水账并失去可用性，跨成员重复必然大量，写入策略（聚类归并成主题 vs 逐条追加 vs 分批）须在专项里先定；③ 配置漂移两处：`_meta/hact-config.md` 登记表的本地路径全是 `E:\group-code\hact-notes-*`（Windows），与当前 WSL 原生环境不符，本次是只读克隆到 scratchpad 绕开的；四个空仓说明"建仓"与"成员上线"之间缺一步。**收割时须去重的已消化项**：本轮虽未走收割，但 dingxifan notes 里 file-extract v1 那批的两条已就地落地——「`draft-prd` 必读清单看不见 V0 落地了什么」已改进 `specs-execution/draft-prd-vN.md`，「`check-sprint.js` 两处子串匹配噪声」已改进脚本本体；专项收割时这两条按"已采纳"跳过，勿重复上提。| 来源：2026-07-30 对话（本欲只处理 file-extract V0/V1 遗留，跑 harvest 时撞出规模远超预期，用户裁定另开专项）
- [x] 2026-07-30 | develop 阶段 B 独审墙钟压缩（扇出并行）——**先量后改，最小变体是"摘最重一类"而非蜂群**：现象=开发 ~20min、独审 40–60min，独审成主要墙钟。两条并行轴不等价：① **跨任务并行**——任务图层面成立（`develop.md:107` 串行的理由是「串行单工作树无写冲突」，管的是**写**、不是只读的审查），但机械上卡在"可并行任务集共用一条 batch 分支、主循环期间不 commit"（commit 在末端一次），`git diff` 是累积的未提交改动，真并行则 A 的审查员与 B 的执行体同时读写同一棵工作树、**审查对象随写入漂移**；解锁前提=batch 分支上 **per-task commit**、审查读 `git diff {prev}..{cur}` 的钉死快照（该步本身独立成立，也让现有串行下"审 B 时 diff=A+B"更干净）。② **单任务内按维度扇出**——`develop-review.md` 八类的输入集基本不相交且成本极不均（第 7 类 DTO 逐字段穷举最重 > 第 6 类设计保真 > 1+4 AC/测试 > 2/3/5/8 轻），切三份并行则墙钟由 sum 变 max；且与 2026-07-12 foundation-review 的结论同向（「brief 严苛度 > 模型大小」＝瓶颈在注意力/指令而非算力，八类塞一个审查员本就在稀释注意力）。**代价（不粉饰）**：(a) 省时不省钱——共享输入 ×N 重读，正面顶决策#26 成本原则，是拿 token 换墙钟；(b) 跨类 finding 会漏（典型：测试忠实 AC 但该字段在 service 被覆盖，横跨第 1 与第 7 类）→ 切分粒度须让每个审查员握 2–3 个相邻类，不可一类一员；(c) 整改 loop 变糊——N 份清单合并后重审跑几个？只跑报阻断的丢了回归、全跑成本再 ×N，须先定规则（如"整改若改动超出该审查员 scope 的文件则全跑"），现有 3 轮封顶随之要重新界定；(d) 误报叠加——brief 写死"存疑即判阻断"，N 个偏保守审查员的假阳性是叠加的，正是 2026-07-14 haiku 回调那个坑换了形状。**最小变体（若要动，先只做这个）**：只把**第 7 类（用户输入去向追踪）**摘成一个并行审查员、其余七类留原审查员——单类最贵、输入集最独立、且输出**自带覆盖率自证**（`行数 < N = 审查未完成`）机械可核、误报面窄；一刀换掉大半墙钟而结构改动近零，(b)(c)(d) 三条代价基本躲开。全面扇出（3–4 员）留到跨任务并行也想要时一并做（那时 per-task commit 反正得补）。**搁置理由**：守 2026-07-08 冻结 + empirical-harvest measure-first——这 40–60min 里"首轮审查"与"整改—重审轮次"各占多少尚未量过；若大头是 2–3 轮 loop，则扇出首轮基本白干，串行的整改才是关键路径。**重启条件**：读 `status.yml` 的 `code_reviews[].rounds`——**判据：绝大多数任务 `rounds=1` → 首轮占大头 → 推进最小变体；`rounds≥2` 占比高 → 墙钟大头在串行整改，扇出首轮基本白干、本条降级为不做**。<br>**2026-07-30 仪器已装**：原写法「在下一个 develop 会话里拆出耗时占比」**当时不可满足**——file-extract v1 实测：`_meta/sessions/` 无 develop progress 文件，`code_reviews[]` 只记终态结论 + 残余建议（10 条全"通过"、issues 全"建议"级），**轮数与耗时事后不可复原**；轮次确实发生过但只散在旁证里（`fe-v1-002` issue 写"末轮已补三落点"、`fe-v1-007` 写"round-1 起既存"、V0 commit 写"3 轮独审"）。故本轮给 `code_reviews[]` 补了 `rounds` 字段（`skeleton/07` schema + `develop.md` 末端当场记）——**仪器须先于实验装，这是 measure-first 的要求而非抢跑**；属既有审计留痕补格、非新增机制。下一个跑完 develop 的迭代（file-extract v2）即产出第一批数据。<br>**2026-08-03 已评估的替代方案·判否（wave 级批量独审）**：用户提"把若干包并成一个 wave、按 wave 发独审与全量检测、不逐包推 PR（前提一次性全取）"。核对现状——`交付=可并行` 任务集**今天已经是 wave**（一次全取 / 共一条 batch 分支 / 末端跑一次全量 / 一个 PR / sprint.md PR 列填同一个 `#N`），净增量只剩「独审合并成 wave 级」与「串行包收进 wave」两项，后者成立、另记新条。**独审合并判否，三条**：① 正面回退决策#24——#24 砍 pr-review 的核心论据就是"per-task 独审比末端批量更早"，wave 审是把批量审换个名字装回来；② **省的是账单小头**——阶段 A（写代码 / 多轮 / 默认模型）是成本大头，阶段 B 只一次 sonnet 调用，合并只省固定前缀（brief 55 行 + 共用 standards 章节），任务包与 diff 是 N 份、合了照读，净省 `(N-1)×(brief+共用 standards)`；③ **注意力稀释正撞在已知瓶颈上**——`develop-review.md` 第 7 类是穷举式（每 DTO 可提交字段一行、禁抽样、末尾覆盖率自证），5 包一员＝一人枚举 5 个 DTO 四十余字段，而 2026-07-12 已实证「brief 严苛度 > 模型大小」＝瓶颈在注意力与指令遵循而非算力。**另两条代价**：批量只在全绿时省钱（一出阻断，重审整个 wave＝省的吐回去，退化 per-task 重审＝批量当场消失）；且 per-task 独审有一项隐藏收益会丢——task 1 审出的系统性错误（如读错 standards）能在 task 2–N 写之前止损，wave 审是同一错误发现 N 次、修 N 个包。**「wave 审用来做跨包一致性」这条退路也堵住**：`develop.md:167` 明确 cross-task 一致性只靠全量绿、不重审，端到端归 GIT，而 2026-07-30 刚判明 GIT 穿透流产出主类型正是**契约漂移**——这一层不缺人。**结论**：压这 40–60min 墙钟的方向是**扇出并行**（保深度、拿 token 换墙钟），不是**批量合并**（省 token、掉深度），因为瓶颈是墙钟不是 token；且本条重启条件（`rounds`）对批量方案的指向**相反**——`rounds≥2` 占比高时扇出白干，批量独审则是纯亏（整改面 ×N）。<br>**2026-08-03 前置发现：扇出在本方法论里目前既不可执行、也不可观测——这是本条真正的前置，先于 `rounds` 数据**：用户反馈"实际执行中基本没见过主动的扇出安排"。核查属实且范围比预想大——方法论**已在 4 份 spec 写了 7 处扇出**（文本 10 处）：`plan-sprint.md:18`＝`:207`（Explore 并行读 6 份输入）、`:208`（Step 3 任务 >4 → 并行 subagent 各写 2–3 个任务包）；`draft-tech-design.md:28`＝`:307`、`:309`（Step 7 两个 subagent 各写一份 standards）；`draft-foundation.md:30`＝`:175`、`:176`（Step 4 standards 播种，同上）；`generate-integration-tests.md:151`（按模块并行派 haiku 跑 pinchtab）。**写了两个月、一次没见它发生**，三条原因全部可核：① **写的是副词、不是可执行指令**——七处全作"并行读 / 并行派"，而本运行时真正决定并行与否的是「**多个 Agent 调用须在同一条 assistant message 内发出**」，全仓 grep「同一条消息 / 一次发出 / 同时发起」**0 命中**；顺序发 N 个 subagent 与并行发 N 个，在规范文本层面同样合规，CC 顺序发完自认已照做。② **没有任何产物能区分二者**——5 个 `check-*.js` 无一核 subagent、`status.yml` 无字段、PR description 无字段；唯一痕迹是 progress.md 的复选框，而 file-extract v1 `_meta/sessions/plan-sprint-v1-progress.md:62`「Step 3 写 10 个任务包（>4 个 → 启并行 subagent）」**至今 `- [ ]` 未勾、而 10 个包早已写完**——既不被勾、也作不了证。这是本仓已命名过的失效类（恒真式度量 / 自报不可信）的极端形态：**连自报都没有**。③ **另一处可能一直在静默失效**：`develop.md:120`「执行 subagent 内部 >5 文件 / 跨模块可再派子 subagent 分模块」依赖 subagent 自身工具集含 Agent（本 harness 的 agent 清单里 `Explore` 明确写 `All tools except Agent`，`general-purpose` 名义 `*`）；若实际配置下 subagent 不能再派，该处**静默退化**为"执行体自己顺序写完所有模块"——不报错、不留痕、规范上仍合规。**一次探针即可证伪**（派一个 general-purpose 问它工具集里有没有 Agent），本轮未跑。**轴②机械可行性已核**：派发点在主线（`develop.md:137`），主线一条 message 内发 N 个 Agent 调用即真并行，且 N 个审查员对同一棵工作树**只读**——无轴①那个"审查对象随写入漂移"的问题，故轴②不卡工作树，卡的正是下面两笔。**据此本条补两笔（均不破冻结）**：**(一) 措辞可执行化**——上列 10 处「并行」各补一句运行时说法「这些 Agent 调用须在同一条消息内发出，分多条消息发出即为串行」，纯措辞修正、零新机制；**它先于任何扇出决策**——不改这句，扇出条目就算推进也只是第 8 处不发生的并行。**(二) 仪器先于实验**（本仓 2026-07-30 自己刚立的规矩）——`code_reviews[]` 跟 `rounds` 并排加 `reviewers`（本任务由几名审查员产出），性质与 `rounds` 完全一致（主线当场知道、事后无处可查）；没有它，`rounds` 数据到手也只能判"该不该扇出"，判不了"扇出到底有没有发生"。**再补一项本条原先漏掉的改动面**：`develop-review.md` 现为「你是一名独立审查员」的单员口吻 + 八类逐类必答 + 单份输出格式，扇出须拆 scope + 定合并规则，**brief 本身要改**（原条只写了 (c) 整改 loop 变糊，没写这个）。**顺带一个观察**：`develop.md` 是主线四份 spec 里**唯一「并行」字样为零**的（仅「可并行任务」「多会话并行」两个无关词义）——墙钟最疼的那份，恰是扇出指令为零的那份。<br>**2026-08-03 数据到手 → 按本条既定判据「降级为不做」**：file-extract v2（11 包，07-31→08-03 全部合并）+ v3-001 实测。**先说仪器**：`rounds` 07-30 落地（`a19bd3d`），v2 全部 11 个包**在其之后**合并，而 `rounds` 记录数 **0/11**，且 `fe-v2-010`/`fe-v2-011` 连 `code_reviews[]` 条目都没有（每任务一条是长期义务、非新规）——**装上的仪器在第一次实战里没响，且无人发现**，与它要解的问题是同一失效类（写了、没人验、于是不发生）。对照 v1 的 10/10 完整，可见漏记是 v2 才起的。**轮数从 `comment` 散文里复原 6 条**（主线把轮次写进了散文、结构化字段空着）：`fe-v2-005`=**4**（三轮撞上 `develop.md:144` 的 3 轮封顶 → 人类裁决收紧范围后再一轮）、`fe-v2-006`=1、`fe-v2-007`=1、`fe-v2-008`=**2**、`fe-v2-009`=**2**、`fe-v3-001`=1；`001`–`004` 的 comment 为 null、`010`/`011` 无条目，5 条不可复原。**判读：`rounds=1` 3 条 : `rounds≥2` 3 条**——**不满足**本条写死的「绝大多数 `rounds=1` → 推进最小变体」，落在「`rounds≥2` 占比高 → 墙钟大头在串行整改、扇出首轮基本白干」一侧。**故本条按既定判据降级为「不做」**（含最小变体），不再等更多数据；若后续某期出现 `rounds=1` 压倒性占比再由该期重开。**顺带记两件规范外的实况**：① `fe-v2-005` 是**三轮封顶规则第一次真被撞到**，而实际处置是"人类裁决收紧审查范围后再跑一轮"——`develop.md:144` 写的是升级到 `revise-doc` 或 escape-hatch，**没有"收紧范围再跑"这条路径**，规范与实践在此有缺口（另议）；② 主线把轮数写进 `comment` 散文说明"当场知道"成立，缺的只是往结构化字段落。**本轮已落机械防线**：`check-sprint.js` 加第 9 项「审计留痕完备性」（详见该条目）。| 来源：2026-07-30 对话（用户提"审查能不能派蜂群扇型铺开"）；2026-08-03 对话补判否 + 前置发现 + 数据判读降级
- [x] 2026-07-29 | Complexity Gate（阶段级复杂度维度）——**思路可取、六项自填不可取**：审查意见提议给每个阶段 Gate 加一道复杂度门（新增纯兼容分支 / 未裁决契约矛盾 / 未批准边界例外 / 重复解析器·状态判断·提示词实现 / 无删除条件的临时开关 / 已被替代仍残留的旧路径，**六项全须为 0**）。**Gate 加复杂度维度是本仓没有的**（现有五关全是正确性/完成度判断），值得记；但**若六项由执行方自填即恒真式度量**——执行方既定义什么算"兼容分支"又统计有几条，只能回答"我列的都归零了吗"，回答不了"我列的是不是全部"（`2026-07-22-evidence-burden-shift` 讨论盘第二层 + 决策#29「risk 不信自报」+ 独审不收执行方自评，本仓已反复付钱）。**要用须改造**：分母来自覆盖方控制不了的独立枚举源——「重复实现」由依赖图工具跑出、「残留旧路径」取 plan-sprint 写的 `supersedes` 字段（非 develop 自列）。另**「复杂度负责人」这个角色与本仓模型冲突**（决策#1/#16 已废角色身份），须翻译成**绑 `architecture` discipline 授权的动作**（同「安全敏感改动须 architecture 裁决」的形状）；「复杂度结论：增加/不变/降低」这类自评栏建议直接不要，两期内必成人人填"不变"的仪式。**搁置理由**：同上条（冻结 + 等重建项目实跑）。**重启条件**：与上条同批评估；届时先看有没有真的独立枚举源可挂，挂不上则本条降级为"不做"。<br>**2026-08-03 评估 → 按本条自己写的条件降级为不做**：原文写死「届时先看有没有真的独立枚举源可挂，**挂不上则本条降级为不做**」。实测 file-extract：确实长出了两个独立枚举源——`verify:architecture`（dep-cruiser 依赖图，覆盖方控制不了）与 `supersedes`（`plan-sprint` 写、非 develop 自列）。**但六项里只有 2 项挂得上**（"重复解析器/状态判断实现" 可由依赖图查、"已被替代仍残留的旧路径" 可由 `supersedes` 查），另四项（纯兼容分支 / 未裁决契约矛盾 / 未批准边界例外 / 无删除条件的临时开关）**仍然只能执行方自填 = 恒真式度量**，正是本条自己判定不可取的那种。**而挂得上的那两项，已经分别由架构墙和退役账在管**——再包一层 Gate 是纯增生，不产生新信息。**故降级为不做**。「Gate 加复杂度维度是本仓没有的」这个观察仍然成立、值得留在 git 历史里；真要复活，前提是另四项各自找到独立枚举源，而不是把六项打包成一道门。 | 来源：2026-07-29 对重建计划的第三方审查意见（用户转述）<br>**2026-07-30 补一条支持"自报不可信"的实证**（file-extract v1 wrap-up 退役账核对）：`fe-v1-005` 的 `supersedes` 自报「`TemplateEngineSnapshot` / `TEMPLATE_ENGINE_PACKAGE` 本包填入真实实现后无调用方」，G5 前逐个实体核实——**只对了一半**：前者确已下线，后者仍在且有两个真实调用方（`package-boundary.spec.ts` 的清单级边界断言 + `apps/api` 的 `API_WIRED_PACKAGES`），且实测**删不得**（删掉这两条依赖边就从模块图上消失，那两个方向的架构墙规则不再被任何真实边检验）。**两点价值**：① 执行方自报的退役声明在首次真实使用中就错了一半——本条"六项自填即恒真式度量"的判断拿到直接实证；② 但**逐个实体核对是可行且当场可判的**（wrap-up 花几分钟就核出来了），说明分母不必来自工具，来自「**独立于自报的核对动作**」也成立——这放宽了本条原写的"须有依赖图工具跑出"前提。
- [x] 2026-08-03 | 依赖闭合时「串行」应塌成一个 wave（`交付` 是**这一刀怎么切**的属性、不是任务的属性）：`plan-sprint.md:84` 定义 `串行` = 单独建分支/PR、**合并 master 后**依赖它的任务才能开 develop 会话——它拿独立 PR **不是它自己需要，而是下游在另一个会话里等它出现在 master 上**。故当一次拾取**依赖闭合**（串行包与其全部下游同在本轮任务集内）时，没有任何人需要那次中间合并，串行 / 可并行的区别当场消失，可合法塌成一条分支 / 一个 PR / 一次全量检测，省掉 N-1 次「末端全量 + PR + 合并 + checkout master + pull」。现状把 `交付` 当**静态字段**写死在 sprint.md（`develop.md:58` 据此令每个串行任务各走一遍完整末端），取全集时即成纯开销。**代价（认，不致命）**：① 回滚粒度变粗——B 坏了 A 一起卡；② 合并时点后移——团队期（第五阶段）别人等 master 的窗口变长，单人期无感；③ 顶着 `develop.md:56` 估量线（末端全量不触发 compact），大 sprint 取不全时自动退化回现状、不必特殊处理。**拟落形态**（改动小）：develop 拾取步加一句判定——本轮取到的串行任务，若无并发会话在等它进 master，则按可并行处理（同分支同 PR）；否则维持现状。<br>**2026-08-03 判定条件修正 + 实证到手**（file-extract v2）：原写「全部下游在本轮任务集内」**条件写错了、按它 v2 一次都不会触发**——`fe-v2-001` 的下游 `005`–`010` 不在那一取里。真正的条件不是依赖闭合而是**并发**：`串行` 服务的对象从定义上就是*另一个并发会话*（`plan-sprint.md:84`），单人顺序作业时它服务的是**空集**。**实证**：`03659af` 一次认领 `fe-v2-001, 002, 003, 004`（四个全是 `交付=串行`，这是 `develop.md:58` 规定的取法），随后各走一遍完整末端 → **PR #12 / #13 / #14 / #15 全部 07-31 当天合并**，四次中间合并**零消费者**（同一个人、同一批，下一个包 `005` 07-31 才认领）。全期账：**11 个任务 → 10 个 PR → 10 次末端全量**（`006`+`007` 因 `可并行` 共用 #17，即现有 wave 按设计生效的那一次），按修正后的条件至少 `001`–`004` 那 4 次可塌成 1 次。**故本条重启条件改为**：不再等"手记一次能省几次"（v2 已给出数），改为等**并发场景是否真出现**——第五阶段团队引入后若出现多人并发 develop，则本条的省法在那时反而危险（中间合并真有消费者），须届时按"是否有并发会话"动态判而非按 `交付` 静态字段判；单人期则纯赚。**2026-09-04 已落地**：`single-operator-wave` 已进入 develop；保留 per-task 独审，补有效 risk 前置/晚升档拆分、per-task commit、整组恢复与机械状态检查。剩余仅观察真实团队并发样本。| 来源：2026-08-03 对话（用户提 wave 批量优化，此为其中成立的一半）；同日补 v2 实证与条件修正
- [ ] 2026-08-03 | batch 分支 per-task commit（钉死独审快照）——**现在就在付批量的代价，却一分批量的钱都没省到**：可并行任务集主循环期间**不 commit**（commit 在末端一次，`develop.md:174-185`），故审 task B 时 `git diff` 拿到的是 **A+B 的累积改动**——今天的"per-task 独审"其实已经部分是 wave 审（scope 糊、审查对象随写入漂移），而批量该省的固定前缀一分未省。修法：batch 分支上 **per-task commit**、独审读 `git diff {prev}..{cur}` 的钉死快照。**独立成立**（不依赖并行 / 塌缩任何一条，只是让现有串行审的 scope 干净），同时是「跨任务并行审」的解锁前提（见 2026-07-30 扇出条 ①）。属既有机制**补格**、非新增机制。**待定细节**：① commit message 形态与末端「批量多任务」格式（`develop.md:180`）的关系——是改成 N 个 per-task commit 共一个 PR，还是保留末端整合 commit；② 断点续做（`develop.md:329-334`）改以 commit 边界判进度，比现在读 `git diff --stat` 更准，一并改。**搁置理由**：守 2026-07-08 冻结（虽属补格，但与 develop 主循环耦合，不单独破冻）。<br>**2026-08-03 单独判：不关，改挂自身理由（原重启条件已失效）**——原文挂的是「与扇出条同批，若那条推进则本条必须先做」，而扇出条同日已判不做，本条成孤儿。**不随之关闭**，因为它的害处独立于墙钟存在，而且比原文写的更具体：`develop-review.md` **第 2 类明令审查员核「是否有超出 `files` 清单的改动」**，而在可并行任务集里审查员拿到的是 A+B+…+k 的累积 diff —— **第 2 个任务起，每个审查员看到的改动天然都超出自己任务包的 `files`**，这是一台结构性的假阳性发生器，且它伪装成"范围蔓延"这种看起来很像真问题的形态。原文只写了"scope 糊 / 审查对象随写入漂移"，没点出这台发生器。**证据强度要说清**：v2 十一个任务里十个是 `交付=串行`（各自分支，不受影响），唯一的可并行对 `006`/`007` 两轮都是"一轮过审"、没触发——**所以是机制推演，不是实测命中，n=0**。**新重启条件**：下一个出现 **≥3 个任务的可并行批次**时，看第二个及之后的任务独审里有没有冒出"超出 files"类 finding；冒出即坐实、按补格落地（batch 分支 per-task commit + 独审读 `git diff {prev}..{cur}`），一个都没冒出则本条关为不做。| 来源：2026-08-03 对话（讨论 wave 批量时暴露）；同日扇出条关闭后单独重判
- [ ] 2026-08-03 | 独审 loop 撞 3 轮封顶后，规范里没有实践真正走的那条路：`develop.md:144` 写的是「同一任务『审查—整改』loop 3 轮仍有阻断 finding → 不再硬磨，**升级**：根因疑在 AC / TRD 设计层 → 起 `revise-doc`；否则走 escape-hatch 浮给用户」。**file-extract `fe-v2-005` 是这条封顶规则第一次真被撞到**，而实际处置是第三条路——`status.yml` 的 `code_reviews[].comment` 原话：「loop 到规范上限后**经人类裁决收紧范围再跑一轮**，末轮修复由主线亲自做变异复验（不采信执行侧自评）」，即**人类裁决缩小审查 scope 后续跑第 4 轮**，既不是 `revise-doc`、也不是把任务退回 escape-hatch 后重派。这条路径在规范里不存在，但它很可能是**对的那条**：3 轮撞顶常见的真因是审查面过宽（八类全开、存疑即阻断）而非 AC/TRD 有病，退回 `revise-doc` 是杀鸡用牛刀、走 escape-hatch 又等于把已收敛 80% 的任务整个丢回去。**待定**：把它补进 `develop.md:144` 作为第三分支（须写清谁有权收紧、收紧的粒度是"砍掉哪几类"还是"缩小文件范围"、以及**收紧后未被审的那部分谁兜**——这是它区别于"放水"的唯一分界，`fe-v2-005` 的做法是主线亲自做变异复验兜底，可作范式）；还是判为一次性例外、维持现状。**注意本条与「扇出并行」条相互作用**：那条已按 `rounds` 数据降级为不做，理由正是墙钟大头在串行整改轮——而 3 轮封顶后的处置恰是整改轮最贵的那一段，本条是同一笔账的另一半。**搁置理由**：守 2026-07-08 冻结，且样本 n=1。**重启条件**：再撞到第二例（`check-sprint.js` 第 9 项落地后 `rounds` 会被机械催记，`rounds≥3` 的条目即为撞顶候选，可直接筛）。| 来源：2026-08-03 file-extract v2 `code_reviews[]` 复盘（查 rounds 数据时撞见）
- [ ] 2026-08-03 | 门卫部署不可复现（hook 是本地状态，且模板↔副本**双向**漂移）：`pre-commit-hook.sh` 装到各仓 `.git/hooks/pre-commit`，**该路径不进版本控制**——今天给四仓（file-extract / doc-extract / JHH-Nortion / mail-ai）做的部署是**这台机器的工作副本状态**，换机器、重新 clone、或新成员上手即消失，且消失时**毫无信号**（commit 照过、绿照绿）。而 `scripts/check-*.js` 是进版本控制的，于是能长出「脚本在仓里躺着、门卫根本不在」的组合——`loxson-salary-new` 正是此形态（4 个脚本 / 0 hook）。**今天的实测证据（三种失效各有实例）**：① **压根没装**——8 个项目仓里 4 个无 hook；② **装了但过期**——doc-extract / JHH-Nortion / mail-ai 跑的是未修版本（`.gitkeep` 误报那版，2026-07-11 早已修好的模板从未下发）；③ **副本反而领先模板**——`file-extract/scripts/check-docs.js` 有个模板没有的 `isExplicitNone()`（允许「涉及实体：无（理由）」的出口），部署时若直接覆盖会毁掉它。**所以漂移是双向的：模板→副本没下发，副本→模板没上收**，两个方向都没有对账动作。最刺眼的单点：`check-reusables.js` 2026-07-30 落地，到 08-03 为止**零仓接入门卫**——它当天找到的那个真命中（JHH-Nortion `reusables.md` 登记的 `StatusSelect` 全仓不存在）是手跑出来的，门卫自己永远不会报；**"落地"与"生效"之间隔着 4 天真空且无人发现**。**落地候选（均撞冻结，故只记不做）**：① `init-project` 铺 hook——一次性，解决不了后续漂移；② 会话启动 Step 0（`templates/CLAUDE.md`，与 `sync_repo` 同址）加一步 hook/脚本版本核对，代价是每次会话多几次文件比对；③ `git config core.hooksPath` 指向**仓内受版本控制**的目录——git 原生、hook 随仓走，但仍需每人配一次、clone 不自动生效；④ 干脆不靠 hook，检查挪进 CI——直接撞上 #19（G3 机械级是否要求"接进自动门"）里判过的"必须另有常驻 CI = 新增基建要求"。**与 #19 的关系要点明**：#19 问的是"机械级红线要不要接进一道真在跑、能拦坏代码进 master 的门"；**本条证明了本方法论现有的那道门（pre-commit）本身就有一半不在跑**——即 #19 的前提被削弱了：在讨论"要不要更强的门"之前，先得让现有的门**可复现**。两条应同批评估，且本条在前。**搁置理由**：四个候选全是新增机制或新增基建，守 2026-07-08 冻结。**重启条件**：与 #19 同批（第五阶段真人 push，届时"新成员 clone 后无门卫"从隐患变成必然）；或更早——出现第一例"换机器/重新 clone 后门卫静默消失"的真实事故即提前重启。| 来源：2026-08-03 对话（四仓门卫部署收尾时，用户点出 hook 不进版本控制）<br>**2026-08-17 复核·状态未变，且多出一个新的等待方**：9 仓抽查，`scripts/check-reusables.js` 六仓有（与模板逐字节相同）、document-query / rms-code / loxsonoa 三仓无；`scripts/pre-commit-hook.sh` tracked 脚本 7 仓有、rms-code 与 loxsonoa 无。**新增第五个落地候选：元检查（meta-check）**——一个独立脚本定期扫全部活跃仓，只答四问：① 各仓 `.git/hooks/pre-commit` 装没装、版本对不对；② 各仓 `scripts/check-*.js` 与模板是否同版（**双向**报，副本领先也要报，防 `isExplicitNone` 那类上收缺口被覆盖掉）；③ B 类任务有无审查留痕（见本清单末条）；④ 各活跃仓距上次 `deploy-log` 写入多少天。它与 ①-④ 候选的区别是**不试图修好门卫，只保证"门卫没在跑"这件事有信号**——现有全部失效（`rounds` 0/11、四仓无 hook、`check-reusables` 零仓接入、B 类零留痕）的共同形态都是**静默**：方法论有大量检查器，但没有一个检查"检查器是否在跑"的检查器。**它仍是新增机制、仍撞冻结**，但性质与 ①-④ 不同（不加约束、只加可见性），且是唯一一个装上后**能自己发现下一个洞**的候选。<br>**同批还有一件已成型但未落地的东西**在等这次分发：零引用组件扫描（见本清单「前端规范一致性框架落地」条 2026-08-17 注）——它改的正是 `check-reusables.js` + `pre-commit-hook.sh` 这两个文件，若门卫分发动手，两者应同批。| 追加来源：2026-08-15 全企业提交分析 + 2026-08-17 对话<br>**2026-09-01 追加·双向漂移最重实例（整期门卫归零）**：awuchi 撞出钩子模板对点号版本号（v1.1 / v1.2）整体失效——`pre-commit-hook.sh` 抽迭代目录与抽版本号的两处正则不含点号段，`for` 循环一次不进，四个检查器整期 no-op（awuchi v1.1：G1–G5 全签、15 任务包、UX 三件套，零次门卫；整数版本一直正常）。**漂移形态是本条 ③ 的变体且更刺眼**：项目侧 2026-08-29 规划期就撞墙并修了**自己仓的** `check-sprint.js` / `check-ux.js` 副本（入参校验 `/^v\d+$/` → 点号版，`61205f8`），但模板与钩子都没动、也没任何人知道——「副本领先模板」这次不是项目自有扩展（`isExplicitNone` 那类），而是**模板自身缺陷的先知修补**，上收缺口直接等价于模板带病运行三天。模板侧 2026-09-01 已修并推送（`143761f`：钩子两处正则 + 两个检查器入参校验 + 畸形目录名兜底警告【staged 有 iterations/ 文件却识别不出迭代目录 → 仅 stderr 警告不拦】+ `init-project` Step 4.1「存量仓重装两跳」说明）；awuchi 本仓钩子已由项目会话自修，**其余存量仓未分发（用户裁决押后）**——本修复应与「零引用组件扫描」同批搭那次分发。候选 ⑤（元检查）论据再加一：若有一台只报信号的元检查，「副本领先」在 08-29 当天就是可见信号，而不是 09-01 靠另一个缺陷被独立重发现才顺带带出。同源第三处（检查器入参校验）当时一并修——只修钩子会从「静默放行」翻成「硬拦提交」（`run()` 对非零退出码一律置 fail，含用法错误 exit 2）。重启条件不变。| 来源：awuchi 项目会话实测 + 跨会话联动修复（2026-09-01）<br>**2026-09-01 同日补·本修复的存量仓分发取消（用户裁决）**：七仓盘点（awuchi / document-extraction / fe-wt-013 / file-extract / jhh-noriton / mail-ai / org-krm-v2），除 awuchi（已自修）外 iterations/ 全为整数版本目录（mail-ai 至 v10，多位整数旧正则本就兼容）——点号缺陷在其余六仓无处触发，正则修复不对存量仓分发。将来某仓首开点号迭代，按 `init-project` Step 4.1「重装两跳」自取模板（模板已修，后续任何分发天然携带新版）。前段「本修复应与零引用组件扫描同批搭分发」就此作废；check-conn / 门卫部署 / 零引用扫描批维持「待用户通知统一做」不变。
- [ ] 2026-08-17 | **B 类任务无审查留痕（20 包 0 报告，其中一仓直发生产）**：2026-08-12~15 全企业提交全量分析实测：走 `b-queue/` + `b-tasks.md` 的 B 类任务**不产生任何 `code-reviews/` 留痕**——`rms-code` 14 包、`document-query` 4 包、`wms-bellwether` 2 包，共 **20 个 B 类包合并进 master，审查报告 0 份**；同期 A 类是 **168 个任务 / 595 份报告**，覆盖完整。`rms-code` 还在同一窗口内做了 13 次 `docs: record production release`（B 类 → 直发生产）。<br>**根因**：决策#29 五层防线里 ④（develop 按有效 risk 定模型档位）⑤（末端安全敏感 diff 独立预检）声称 source 无关、同时兜 B 类，但这两条都是**运行时行为、不落痕迹**；而 ①②③ 明确住 A 类 G3 链（填包规则 / plan-sprint Step 3.5 第⑥类 / `check-sprint.js`）。**留痕这一项，五层一层都没覆盖到 B 类**。物理上也缺位置：`code-reviews/` 挂在 `iterations/vN/` 下，而 B 类无迭代。<br>**后果**：「这个 B 包到底跑没跑独审」事后**无法从仓库判定**。决策#24 用 per-task 独审换掉了人工 merge 门，其可举证性是这笔交易的对价；A 类举得出、B 类举不出，而两者进同一个 master、同一台生产机。企业级审计口径下，一半有留痕比全都没有更糟——它制造覆盖完整的假象。<br>**这是同类第二例**：2026-08-03「仪器装了没响」（`rounds` 07-30 落地、v2 十一包全在其后合并却 0/11 记录）是第一例。那次是一个字段没写，这次是整个审查产物目录在 B 类路径上不存在，范围更大。两次的共同形态都是**静默**——见本清单「门卫部署不可复现」条 2026-08-17 注的「元检查」候选。<br>**待判的第一个问题**（动手前先答）：补 B 类留痕算**给既有机制补格**（决策#24 的留痕义务本就该 source 无关，只是落地时漏了 B 类）还是**新增机制**（守 2026-07-08 冻结）？倾向前者——决策#24 写的是「审计留痕 `code_reviews[]` 改由 develop 末端写」，没写「仅限 A 类」，B 类无留痕更像实现缺口而非设计选择。若判为补格，则冻结不构成阻碍。<br>**落点候选**（未评估，仅记）：① `code-reviews/` 之外给 B 类开 `b-reviews/{task-id}/`（`templates/CLAUDE.md` 已提到 `b-reviews/{task-id}/preflight.md`，说明这个路径**已被规范引用但无人产出**，值得先核对该引用是否早已落空）；② 或统一写进项目根 `status.yml` 的 `code_reviews[]`，不落文件；③ 机械核对交给元检查而非门卫（B 类合并时点分散，门卫拦不住）。| 来源：2026-08-15 全企业提交全量分析（08-12~15 窗口，1,157 commit / 21 活跃仓 / 11 人）
