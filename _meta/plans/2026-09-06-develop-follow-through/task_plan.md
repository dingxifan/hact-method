# develop 已授权任务集持续推进

## 范围

用户确认：先修 develop 内部主线接续、blocked 分流、结束判据与恢复记录。改本仓 develop 规范、为 progress 配套的工作树检查参数及研发记录；不改其他 task.type、全局启动协议、运行时映射、个人 skill 或项目仓。指定 layer 不扩大为清空该层队列，本轮不实现跨批续取。

## 工作项

- [x] 核对当前计划、接口、develop 执行/结构契约和直接相关待议条目。
- [x] 修改主线接续与 blocked 恢复顺序，保留现有上限及人工门。
- [x] 将完成声明置于必要收尾之后，扩充既有 progress 的恢复信息。
- [x] 补工作树检查器的精确 progress 参数与正反夹具，避免新记录自阻断。
- [x] 做场景式规则走查与现有相关检查，记录实测边界。

## 不变量

- Gate、前端设计确认、安全敏感合并裁决、隔离审查和固定快照不降级。
- 重派/恢复不清零测试修复次数或 code rounds；达到上限仍升级。
- 不增加任务状态枚举、全局 autopilot 文件或后台调度器；旧 progress 可恢复，wave JSON schema 保持原样。
- 不以静态检查宣称真实长程模型行为已验证。
