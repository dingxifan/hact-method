# 实证回收 findings（2026-07-08）

> 样本：mail-ai + JHH-Nortion（各含旧方法/新方法分期）。方法：判据先立后挖。
> 数据源：两项目 status.yml / status-history.yml / b-tasks.md / iterations/*/gates.md / gate-checks/ / acceptance-report.md / deploy-log.md / _meta/sessions/ / git 历史。

## §0 判定判据（先于挖掘写定，挖掘中未改）

对每个机制（判官检查项 / 独审维度 / 人工确认点 / 流程步骤），按其**已部署迭代**内的记录判定：

| 判定 | 条件 | 处置 |
|------|------|------|
| **裁减候选** | 零触发（未拦住任何东西、未产生人工改判），且它防的失效模式在缺陷归因中也未出现 | 降级出规范正文，退回待议（保留召回条件） |
| **保留** | ≥1 次真实拦截（阻断后产物确实被修改），或其防的失效模式在逃逸缺陷中出现过 | 不动 |
| **改造候选** | 有触发但全部为误报/空转（触发后无产物修改），或其目标失效仍成批逃逸 | 改判定规则，不是删 |
| **加投方向** | 缺陷归因中某一层反复漏（≥3 个 bug 归因到同一缺失能力） | 下一笔主投入 |
| **数据缺口** | 现有记录答不了 | 列入"下轮迭代起补记"清单，不臆断 |

成本曲线判定：新方法分期相对旧方法分期，若"单位功能交付的历时/返工"未改善甚至恶化 → 复杂度收益存疑。

## §1 版本考古（迭代 × 机制存在性）

**mail-ai**：4 月为无方法期（v1–v4 一次性建成，"initial deployment commit — v4 complete" 2026-04-08；协调文件 5/16 迁入 hact-solo 结构时**回填**）→ 6/19 切 hact-method。
**JHH-Nortion**：v1–v6 为 hact-solo 期（GA 日期 4/12–5/3，协调文件同样 5/16 回填）→ v7 起 hact-method。

| 迭代 | 日期 | 当期方法论状态 |
|------|------|---------------|
| mail v5 | 6/19–21 | 冷核 subagent 期（gate-checks/ 留档 4 份）；develop 执行模型翻转（6/20）发生在迭代中途 |
| mail v6 | 6/21–22 | check-gate.js 机械化上线（v6 G3 部署）、merge-on-push 生效 |
| mail v7 | 6/23–24（mt 至 6/27） | 同上；review-briefs 外置后 |
| mail v8 | 6/28–29 | 视觉地基三件套**当期落地**（事故即本迭代） |
| JHH v7 | 6/21–27 | merge-on-push 期；Next.js 栈（栈剥离问题的实际来源） |

**从未部署**（晚于两项目最后一轮 A 类迭代，本轮不可判定）：决策#25 V0/foundation、#26/#29 risk 链（53 个任务包中 `risk` 字段出现 0 次）、#27 栈子模板、#28 ⚖️ 分级。

## §2 机制触发率/拦截率（仅新方法分期）

| 机制 | 部署范围 | 触发/拦截记录 | 判定 |
|------|---------|--------------|------|
| **develop 阶段 B 独立对抗审查** | 全部 5 轮 A 类 + B 类 | **≈70 条阻断性 finding 全部修复后合并**：mail v5≈10（002/003 各打回 4 阻断）、v6≈16（含 mt 任务）、v7=26（TTL 缓存缺测/upsert 违禁/decrypt 静默降级/Retry-After/RolesGuard 缺测/循环依赖…）、v8=0、B 类≈15（b-010 一轮 6 阻断；b-006 Enter 双触发/Esc 卡死）；JHH v7=11（含 **RLS 漏写 user_id**、409 error 被全局 filter 吞、**单测假绿**连带修复、TRD 未定义字段拦截）。误报记录仅 1 条（b-010 AVATAR_COLORS）。 | **保留**——全体系头号价值机制，证据压倒性 |
| **安全敏感人工裁决（#24 escape hatch）** | v6 起 | 真实触发 2 次：v6-003"人工裁决数据隔离校验合规后合并"、b-001 认证改动 3 轮审查+安全专项；v8 预检 8/8 逐任务出具"不触及四类"结论 | **保留**——低频但真触发，且预检在逐任务留痕 |
| **manual-test 验收循环** | v5 起 | ≈19 次拦截：v6 产出 mt-001~005 五个修复任务；v7 问题记录 6 条（含 2 个**真实外部 API bug**：飞书 OIDC token 调用方式错、HTTP 204 拦截器误判）；v8 八批视觉直修 | **保留**——第二价值层；同时暴露上游漏的三类（见 §3） |
| **generate-integration-tests** | v5 起 | 118 场景仅 2 次失败拦截（如 v7 A1）；绝大多数为确认性通过 | **改造候选**——拦截率极低；其盲区（mock 外部 API、不碰真实客户端）恰是逃逸 bug 主产地，应向"真实外部边界冒烟"改造（与 §3 加投方向合流） |
| **判官脚本（check-docs/sprint/gate）+ 门卫 hook** | 两项目均已铺 | 仅 1 条留痕（draft-tech-design：check-docs 32 pass/0 fail）；运行结果**不留痕**，拦截率不可判 | **数据缺口**——补记后下轮判 |
| **Gate 冷核 subagent** | mail v5 | v5 留档 4 份（判据逐条 pass+证据）；v6 起被 check-gate.js 机械化替代 | 已按方法论自身演进退役，无需处置 |
| **escalate-if 字段** | 全部 53 包 | 0 次触发记录；唯一偏离走的是 unmet-ac 降级（v8-004，记 feedback 闭环）✓ | 零触发但字段成本≈一行/包，**保留**，下轮复查 |
| **api-contract 字段** | v5 起 | 在用；但 v7 mt#2（admin 列表 camelCase/snake_case 契约漂移）恰是其目标失效且逃到 manual-test——漂移发生在**未列入任务包 api-contract 的实体直传接口**上 | **改造候选**——覆盖盲区：契约约束只护"包内声明的接口"，实体直传/隐式序列化不在网内 |
| **status.yml 契约** | v5 / JHH v7 起 | 设计消费者（hact-app）仍未切读；但本次审计全程以它为主数据源——**首次真实消费成立**，code_reviews[] 留痕质量高；JHH 已按 #26 归档约定分流 status-history.yml ✓ | 保留；全-5 的"无消费者"论断部分修正（审计消费≠应用消费，切读仍待做） |
| **b-tasks 总账 / deploy-log** | 全程 | 23 条 B 类全程可溯（PR 号/CR/日期）；deploy-log 含健康检查+回滚基线 | **保留**——本次归因分析全靠它们 |

**v8 独审阻断归零的解读**：v7 后端新模块（auth/admin/feishu）阻断 26 条 → v8 前端+配置 0 条。可辨因素：v8 任务类型更轻、standards/notes 到 v8 已累积四轮、视觉地基当期上线。但 v8 的 manual-test 仍抓出八批视觉偏离——独审对"视觉保真"维度基本失明（其 finding 全部是逻辑/测试/契约类），视觉问题整体后移到 manual-test 层消化。

## §3 缺陷逃逸归因（逃到生产/验收后的 bug，按今天的管线归因）

mail-ai 生产期 B 类 bug 8 条（JHH 0 条 bug）：

| bug | 归因层 | 今天的管线能拦吗 |
|-----|--------|----------------|
| b-015 Outlook 内嵌图红叉 | 外部客户端互操作 | ❌ 无外部客户端矩阵 |
| b-019 IMAP 静默丢邮件（唯一 hotfix） | 真实数据规模 + **静默失败无告警** | ❌ 无生产可观测性；列宽问题仓内测不出 |
| b-018 OSS 大附件 60s 超时 | 真实数据规模/环境配置 | ❌ 仓内无大附件真实流量 |
| b-016 replyAll 自己看不到回复 | 产品语义缺口（AC 从未定义） | ❌ 输入侧再严也拦不住没写的需求 |
| b-014 已发送草稿不消失 | 产品语义缺口（状态流转 AC 缺） | ❌ 同上 |
| b-005 ComposePane 误注入原文 | 前端交互边缘（mock 盲区） | ⚠️ 行为化 AC + 视觉冒烟可能拦 |
| b-012 多封 thread 正文不可见 | 真实数据形态（多封聚合） | ⚠️ 真实数据冒烟可能拦 |
| b-001 登录态持久化 | 实为功能诉求（标签为 bug） | 不计 |

另 v7 manual-test 抓住（未逃逸但同族）：飞书 OAuth 真实 API 调用错误（单测 mock 故漏）、HTTP 204 拦截器误判（浏览器路径盲区）。

**归因聚类**（按 §0"≥3 bug 同一缺失能力 = 加投方向"）：
1. **真实外部边界**（Outlook / 飞书 / IMAP 真实数据 / OSS 规模）：≥5 例 → **命中加投判据**
2. **生产可观测性**（b-019 的"静默"是唯一 hotfix 的根性质）：1 例但代价最高
3. 产品语义缺口（PRD/AC 层）：2 例——属 draft-prd 完备性，非执行层
4. 输入侧规范/审查能拦而漏的：**≈0 例**

**结论**：执-4（输出验证侧薄）被证实，但方向收窄——不是"更多仓内集成测试"（其拦截率已证 2/118），而是**真实外部边界冒烟 + 生产静默失败告警**。

## §4 成本曲线

- **新方法节拍稳定且未随方法变重而变慢**：mail v5–v8 每轮全 Gate 2–2.5 天 / 8–10 任务包（v5:69 commits/16% fix → v6:98/32%（含 mt 修复）→ v7:99/21% → v8:37/22%）。6/19–6/29 期间方法论本身经历冷核→机械化→develop 翻转→视觉地基四次大改，迭代历时无恶化。
- **新旧对照受限（诚实声明）**：两项目旧期协调文件均为 5/16 回填，旧期无 bug 总账/CR 记录 → **质量维度不可量化对照**；吞吐对照（JHH 旧期 6 迭代/3 周 vs 新期 1 迭代/7 天）因迭代规模不可比，不给结论。
- 定性证据两条（方向支持新期质量更高，但非量化）：6/22 JHH 旧期代码审计书面结论"规范无防线 + 注册表不可信"；mail 无方法期（4 月）代码是 b-018/b-019 底层缺陷的宿主。

## §5 判定表与建议

| 判定 | 对象 |
|------|------|
| **保留（强证据）** | 独审 loop（70 阻断）、安全裁决 hatch（2 触发）、manual-test 循环（19 拦截）、b-tasks/status-history 留痕与归档 |
| **改造候选** | ① generate-integration-tests → 向真实外部边界冒烟改造；② api-contract → 补"实体直传/隐式序列化"盲区（或由 TRD 层出全量契约对账） |
| **零触发但廉价，保留复查** | escalate-if 字段 |
| **不可判定（未部署库存）** | #25 V0/foundation、#26/#29 risk 链、#27 栈子模板、#28 ⚖️ 分级——**各自首个部署迭代结束后回收一次即判** |
| **加投方向（唯一）** | 输出侧：真实外部边界冒烟 + 生产静默失败告警（b-019 型）。产品语义缺口两例另归 draft-prd 完备性观察，不立即动 |

**对"复杂度是否超过收益"的总回答**：已部署机制的大盘触发率高、死重极少（仅 escalate-if 零触发，且近零成本）；复杂度的可疑部分**全部集中在 6/29 之后新增且从未部署的库存层**（#25–#29）。方法论不需要收敛已部署部分，需要的是：**冻结新增，让库存过一轮真实迭代再判**。

**下轮迭代起补记清单**（数据缺口）：
1. 判官/门卫每次运行结果一行留痕（进 iterations/vN/gate-checks/ 或 session 日志固定段）
2. ⚖️ 默认判定被用户推翻的次数（#28 部署后）
3. 独审 finding 的误报标记（当前仅靠 comment 散记）
4. risk 字段实际分布与升档次数（#29 部署后）
5. token/成本计量（如计费侧可导出，按 task 归集）
6. **Haiku 降档独审的漏网率**（#26/#29 部署后）：`risk: standard` 独审跑在 haiku 上——记录 haiku 审过的 standard 任务里，事后被 manual-test/生产逮出的漏网 bug 数（尤其非安全的正确性/假绿测试类，这格不在 #29 安全网内）。缘由：本环境 risk 链从未部署（53 包 risk 出现 0 次），"haiku 是否系统性漏网"无法从存量回答，属前向仪表化，与第 3/4 项（误报标记/risk 分布）同族。判据：若量出 haiku 在 standard 正确性上系统性漏网 → 先试"用 brief 严苛度补"（develop-review 测试保真检查写成显式对抗动作，同 foundation-review 证据化收紧的杠杆），补不上再议升档（改 #26，走破冻评估）。| 附带自 2026-07-12 foundation-review 证据化收紧讨论

**guide 漂移（全-3 顺手项）**：本轮未动，仍待小修。
