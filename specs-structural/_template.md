# task: {task-type}

**discipline**: {discipline}
**Gate**: {G1–G5 / —}
**属性**: {属性名}={取值范围} · {属性名}={取值范围}

> 一句话描述：这个 task 做什么。

---

## 前置条件

> 触发场景（什么情况下会创建这个 task）+ 硬性前置（Gate 状态、文件是否存在、依赖 task 的状态）。
> 前置不满足时，task 不应进入 [可取] 状态。

- **触发**：...
- **Gate**：...（未满足则阻断）
- **文件**：...（不存在则阻断）
- **依赖 task**：...（未 [merged] 则阻断）

---

## 字段规范

> 所有属性字段的完整定义：取值范围、格式约束、必填性。

| 字段 | 类型 | 必填 | 取值 / 说明 |
|------|------|:----:|------------|
| `field-name` | string / enum / string[] / URL | ✅ / — | ... |

---

## 工作内容

> 步骤式。每步说明：做什么 + 关键约束 + 产出什么。

1. **步骤名**：...
2. **步骤名**：...

---

## 主要产物

> 文件路径 + 格式约定。

| 产物 | 路径 | 格式 |
|------|------|------|
| ... | `path/to/file` | ... |

---

## 完成判据

> 精确的 done 条件。全部满足才能将 task 状态推进到 [done] / [merged]。

- [ ] ...
- [ ] ...

---

## 接口约定

**输入来自**

| 上游 task | 交接内容 | 格式 |
|-----------|---------|------|
| `{task-type}` | ... | ... |

**输出给**

| 下游 task | 交接内容 | 格式 |
|-----------|---------|------|
| `{task-type}` | ... | ... |

---

## 边界场景

> 预期的正常变化情况（不是异常），及对应处理方式。

| 场景 | 处理方式 |
|------|---------|
| ... | ... |

---

## 异常处理

> task 失败 / 阻塞时的上报和恢复路径。

| 情况 | 处理方式 |
|------|---------|
| ... | ... |
