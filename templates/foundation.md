# 地基蓝图 · {项目名}

> 本文件是全项目「跨切面地基」的唯一参照：哪些关注点必须在地基层一次性立住、每块的强制边必须达到哪一档、核心领域模型长什么样。
> 由 `init-project` 的「项目共识讨论」步生成初稿（读 `_meta/input/` 背景材料、**只摸全貌与领域模型，不列功能**），下游消费：
> - **V0 走骨架**（`draft-foundation` 地基设计 + `develop` source=foundation）按本文件选具体形式、把骨架建出来、按「应有档」验收；
> - **draft-prd** 功能挂「一、领域地图」的核心实体；**draft-ux** 把视觉真值填进 V0 已建好的主题框架。
> **活文档**：v1 播种、vN+1 原地增补（沿用 standards 归位模式），不求一次写全；含糊的留空，等开发中"跳出地基"再提拔进来。
> **按项目尺寸伸缩**：中小型项目半页即可——「一」几行、「二」勾选技术内生 + 补一两条领域涌现。完成判据是"地基播了种、各方认了"，不是"全貌全想透"。
>
> **成功度量（命门）**：地基建得好不好，不看技术多新、封装多漂亮，只看一个概率——**「一个图省事的人、不刻意守规范、顺手写出来的东西，有多大概率合规？」**。规矩靠文档=赶工期没人看；唯一真起作用的逻辑是**把"图省事"和"守规范"变成同一条路**。「二」表的「应有档/实际档」就是逐块给这个概率打分（构造级≈100% > 机械级 > 人审级），**能往上一档就别停下一档**——这就是在把那个概率往上推。

---

## 一、领域地图

> 这个应用"是什么、解决什么"里**贯穿全局**的东西——功能都挂在它上面。领域涌现型地基的来源。

**核心实体 / 主数据**（贯穿全局、几乎所有功能都依赖的少数几个）：

| 实体 | 它是什么 | 谁依赖它 |
|------|---------|---------|
| | | |

**贯穿全局的作用域**（所有数据/操作是否都绑某个范围？租户 / 组织 / 用户 / 项目……）：

- 作用域 = `<待填，如 user_id / account_id 二元组>`（无则写"无"）

**贯穿全局的业务状态机 / 关键不变式**（每个功能都得遵守的，如有）：

- `<待填 / 无>`

---

## 二、地基关注点登记 + 强制边诊断

> 一张表管到底（不在别处重复）。**技术内生**几行已预置（任何这个栈的应用都有），讨论时勾选/删减；**领域涌现**从「一」补进来。
> - **形式**：说它（共享类型）/ 穿过它（单例瓶颈）/ 住进它（外壳）/ 被它笼罩（环境基线）/ 往里填（脚手架）——决定于"业务代码怎么碰它"。
> - **应有档（init 立 bar）**：构造级（违规写不出来）> 机械级（写得出但 lint 红）> 人审级（靠人看）。**能往上一档就别停下一档。**
>   ⚠️ **安全敏感项（数据隔离 / 鉴权 / 越权）应有档 = 构造级，不可议价**——成熟项目实证：bar 不在上游立，再好的团队也默认滑到手写弱边、漏一行就泄数据。
> - **形式/实际档·落点**：init 留空或暂记，由 V0 `draft-foundation` 选定具体技术形式后填；develop 的 foundation-review 拿"实际档 vs 应有档"验收，差距即打回。

| 地基关注点 | 落点 | 形式 | 应有档（init 立） | 实际形式·档（V0 填） |
|-----------|:---:|------|:---------------:|:------------------:|
| **数据隔离 / 作用域** | 后端 | 穿过它（作用域 base repo / 查询层） | **构造级** ⚠️ | |
| **鉴权 / 越权** | 后端 | 穿过它（全局卫士 + 拦截器） | **构造级** ⚠️ | |
| 错误信封 / 异常处理 | 后端 | 穿过它（全局异常 filter）+ 说它（错误词汇） | 构造级 | |
| 请求管道 / 响应包装 | 后端 | 穿过它（拦截器） | 构造级 | |
| 入参校验 | 后端 | 穿过它（校验管道） | 构造级 | |
| 分页 / 列表协议 | 后端 | 说它（信封类型）+ 穿过它 | 机械级 | |
| 序列化格式（时间/金额/ID） | 后端 | 说它（共享类型） | 机械级 | |
| 页面外壳（导航/布局） | 前端 | 住进它（AppLayout） | 构造级 | |
| 视觉地基（reset + 主题覆盖 + token 接线） | 前端 | 被它笼罩（全局样式入口） | 机械级（lint 禁裸值） | |
| 状态四态（加载/空/错/成） | 前端 | 往里填（数据页脚手架） | 机械级 | |
| HTTP 单例（拦截器注入 token/作用域） | 前端 | 穿过它（api client） | 构造级 | |
| `<领域涌现项，从「一」补>` | | | | |

> **走骨架（V0）= 把上表每一行最小化地各立一根，串成穿透前端→API→DB 的空壳应用**（不干实事，但每面承重墙就位）。纪律：**只立承重墙、不装修**——一根穿透到底的最薄切片证明架构，不建投机的完整框架。
