# 视觉规格 · {项目名}

> 本文件是全项目视觉实现的唯一参照，由 `draft-ux` Step 1.3（首次 UX 时）生成初稿、逐迭代维护。
> 填写后由前端开发在 `src/styles/variables.scss` 中定义对应 SCSS 变量。

> **可切片读取**：以下第〇至第七节共同构成稳定的「全局视觉基线」，每个前端任务都读；页面特有的布局、控件组合和状态规格追加到文末「八、页面规格」，每页一个 `### {页面名}` 稳定标题。任务包 `reference` 点名该标题后，G4 只读全局基线与该页；旧文件没有这一结构时如实回退全文，不猜测切片边界。

---

## 〇、视觉冒烟锚点（机械断言，供 generate-integration-tests 实测比对）

下列地基不变量在联调**完整档**由浏览器场景执行能力通过 JS **实测比对**（不符即 `[阻断]`）——把"靠人眼"的视觉地基从 manual-test 末端前移成自动校验。值随下方各系统填好后在此登记：

| 锚点 | 实测方式 | 期望值（填写后登记） |
|------|---------|---------------------|
| 主色已覆盖 UI 库 | `getComputedStyle(document.documentElement).getPropertyValue('--el-color-primary')`（按本项目 UI 库主色变量名调整） | == 「一、色彩系统」`$primary` 色值 |
| 目标视口无横向外溢 | 主要页面、目标视口宽下 `document.documentElement.scrollWidth - window.innerWidth <= 0` | ≤ 0 |
| 关键容器尺寸 | 侧栏宽 / 顶栏高等的实测 `offsetWidth`/`offsetHeight` | == 「六、布局规范」对应 token |

> 锚点只覆盖**少数确定可断言**的地基不变量（主色 / 外溢 / 关键容器），不追求逐像素；逐像素与字号字重观感仍由 manual-test 人工走查。

---

## 一、色彩系统

### 主色

| 变量 | 色值 | 用途 |
|------|------|------|
| `$primary` | | 主按钮、选中状态、强调元素 |
| `$primary-light` | | 选中背景、hover 状态 |
| `$primary-dark` | | 按钮 hover |

### 背景色

| 变量 | 色值 | 用途 |
|------|------|------|
| `$bg-page` | | 页面背景 |
| `$bg-card` | | 卡片背景 |
| `$bg-hover` | | 悬停背景 |

### 文字色

| 变量 | 色值 | 用途 |
|------|------|------|
| `$text-primary` | | 主要文字 |
| `$text-secondary` | | 次要文字 |
| `$text-hint` | | 辅助文字、占位文字 |

### 边框色

| 变量 | 色值 | 用途 |
|------|------|------|
| `$border-color` | | 统一边框 |

### 状态色

| 变量 | 色值 | 用途 |
|------|------|------|
| `$color-success` | | 成功 |
| `$color-warning` | | 警告 |
| `$color-danger` | | 危险/删除 |
| `$color-disabled` | | 禁用/已完成 |
| `$color-info` | | 链接、进度 |

---

## 二、字体系统

### 字体家族

| 用途 | 字体栈 |
|------|------|
| 基础 | |
| 数据/数字 | |

### 字号与行高

| 级别 | 字号 | 行高 | 字重 |
|------|------|------|------|
| H1 | 32px | 1.4 | 700 |
| H2 | 24px | 1.5 | 700 |
| H3 | 18px | 1.6 | 500 |
| 正文 | 16px | 1.8 | 400 |
| 说明文字 | 14px | 1.6 | 400 |
| 辅助文字 | 12px | 1.5 | 400 |

---

## 三、间距系统（8px 网格）

| 变量 | 值 | 常用场景 |
|------|----|---------|
| `$space-xs` | 4px | 紧凑内边距、图标间距 |
| `$space-sm` | 8px | 标签内边距、行内间距 |
| `$space-md` | 16px | 卡片内边距、表单行距 |
| `$space-lg` | 24px | 区块间距 |
| `$space-xl` | 32px | 页面区块间距 |
| `$space-xxl` | 48px | 大区块分隔 |

> 禁止使用以上列表之外的间距值。

---

## 四、圆角与阴影

### 圆角

| 变量 | 值 | 用途 |
|------|----|------|
| `$radius-sm` | | 标签、小型控件 |
| `$radius-md` | | 卡片、按钮 |
| `$radius-lg` | | 抽屉、弹窗 |
| `$radius-full` | 9999px | 胶囊形状 |

### 阴影

| 变量 | 值 | 用途 |
|------|----|----|
| `$shadow-sm` | | 列表悬浮 |
| `$shadow-md` | | 卡片 |
| `$shadow-lg` | | 弹窗、抽屉 |

---

## 五、过渡动效

| 变量 | 值 | 用途 |
|------|----|----|
| `$transition-fast` | `0.2s ease` | 颜色切换、hover |
| `$transition-base` | `0.3s ease` | 展开/收起 |
| `$transition-slow` | `0.4s ease-out` | 抽屉滑入 |

---

## 六、布局规范

### 结构常量

| 元素 | 值 |
|------|-----|
| 顶部导航高度 | |
| 侧边栏宽度（展开） | |
| 侧边栏宽度（收起） | |
| 页面内容最大宽度 | |

### 响应式断点

| 断点 | 值 | 行为 |
|------|-----|------|
| Mobile | | |
| Tablet（核心断点） | | |
| Desktop | | |

### 响应式规则

- 移动端弹窗宽度：`95vw`，必须加 `align-center`
- 移动端右侧抽屉宽度：`100%`
- 输入框 `font-size` 移动端不低于 `16px`（iOS 会触发页面放大）
- 按钮最小点击区域：`44px × 44px`

---

## 七、控件尺寸规范

### 输入框宽度

| 变量 | 值 | 场景 |
|------|----|------|
| `$input-width-sm` | | |
| `$input-width-md` | | |
| `$input-width-lg` | | |
| `$input-width-full` | 100% | 满宽 |

### 按钮高度

| 变量 | 值 |
|------|----|
| `$btn-height-sm` | |
| `$btn-height-md` | |
| `$btn-height-lg` | |

---

## 八、页面规格

> 每个实际页面一个三级标题，记录只属于该页的布局、区块、控件尺寸、空态/错误态视觉和响应式例外；不得重复全局 token。弹层、抽屉和页面内区域写在所属页面下，不升格成页面标题；其打开来源与返回位置以 ux-flows 的动线—界面地图为准。任务包以页面标题作稳定锚。

### {页面名}

{服务的 U-id / 进入与返回位置 / 布局及信息顺序 / 状态与反馈 / 未保存与恢复行为；只写该页特有约定}
