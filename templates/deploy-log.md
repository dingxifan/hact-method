<!--
  部署日志模板 · deploy 产物 · 存项目根 deploy-log.md · live 引用（不入项目仓）
  人看的视图（机器侧状态以项目根 status.yml 为准，见 skeleton/07）。
  每次部署**追加**一条（不覆盖）；时间 + target + 部署人作标题。
  构建/健康检查按实际结果填：成功路径填「成功」/「通过」，失败路径填「失败（原因/现象）」。
  备注无则省略整行。
-->
## {YYYY-MM-DD HH:MM} · {target} · {部署人}
- 包含内容：{A 类 vN / B 类 {task-id 列表} / hotfix {task-id}}
- 构建结果：成功 / 失败（{原因}）
- 健康检查：通过 / 失败（{现象}）
- 备注：{有则填}
