<!--
  TRD 结构化模板 · hact-method
  规则（draft-tech-design 结构层契约 + check-docs.js linter）：
  - 7 个必含段落用下列固定 `## ` header，不可缺、不可空。
  - 数据库设计段每张表用固定 header `### 表：{表名}` 切块（表名与 PRD `涉及实体` 同名同形，大小写不敏感）。
  - 接口设计段每个接口用固定 header `### 接口：{方法} {路径}`，槽位齐：请求体 / 响应体 / 错误码 / 服务流程。
  - 凡 `<待填>` 必须替换为实际内容（留着 = linter FAIL）。无内容的段（如本期无新依赖）写"无"，不留 `<待填>`。
  - 提示写在 HTML 注释里（linter 忽略）；删掉示例占位块后再交付。
-->

# TRD · v{N} · {项目名}

## 技术选型变更

<!-- 仅写本期新增依赖；格式：依赖 / 用途 / 选型理由。迭代项目不重复已有选型；本期无变更写"无"。 -->
<待填>

## 数据库设计

### 表：{表名}
- 字段：<待填>
- 约束 / 索引 / 外键 / 唯一性：<待填>

<!-- 复制上面整块「### 表：」追加更多表。PRD 每个涉及实体须在此有对应表。 -->

## 接口设计

### 接口：{GET/POST/...} {/path}
- 请求体：<待填>
- 响应体：<待填>
- 错误码：<待填>
- 服务流程：<待填>
# 满足 AC：<待填>

<!-- 复制上面整块「### 接口：」追加更多接口。
     服务流程：服务于哪条 ux-flows 场景路径；无 UX 的接口写所属功能。
     # 满足 AC：本接口承接的 PRD AC id（如 AC-01、AC-03），多条逗号分隔；纯内部逻辑无 AC 写"无"。
     check-docs 全局扫此行做 PRD AC↔回链逐条对账——载体无关，模块/交互场景旁也可写同样的 `# 满足 AC：` 行（见下）。
     测试脊柱幕 2：不可视区 AC 的行为例子源自 PRD（幕 1），在回链行旁把它精化为技术精确规格——
       例：Given 无权限用户 / When POST 本接口 / Then 返回 403 + 错误码 PERM_DENIED。
       只补技术精度（状态码/错误码/断言），不创造新例子；不在此写 runnable 测试文件（物化守 develop）。 -->

## 测试环境约定

<!-- 不可省略：后端地址 / 前端访问方式 / 数据库指向 / 有副作用操作的禁止清单 -->
<待填>

## 交互技术方案

<!-- PRD 交互中有技术含义的部分：轮询 vs WebSocket / 复杂状态流转 / 跨模块数据共享等。无则写"无"。 -->
<待填>

## 模块拆分

<!-- 前后端各自的模块划分，明确各模块职责边界。
     纯前端交互类 AC（无后端接口可挂）在对应模块旁写 `# 满足 AC：AC-nn` 回链，
     让 check-docs 能机械核到它已被承接（也可写在 ux-flows 场景旁）。 -->
<待填>

## 共享组件建议

<!-- 参照 reusables.md 避免重复建议；本期无则写"无"。 -->
<待填>
