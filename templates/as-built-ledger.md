# As-built 对账账本 · v{N}

> 只记录本期需求触及的 V0 地基事实；不复制整仓结构，不把摘要当权威原文。

## 证据源

- `reusables.md` §标杆切片登记
- `{迁移/实体/状态机/队列定义的实际路径与符号}`

## 对账项

| 对象 | 设计声明 | as-built 证据 | 实际观察 | 关系 | 动作 |
|---|---|---|---|---|---|
| {实体/枚举/队列/转换边/地基关注点} | {foundation/project/上期契约中的声明} | `{路径#符号或迁移 id}` | {当前真实承载与约束} | 一致 / 漂移 / 未知 | {沿用；或 revise-doc id；或需补证据} |

关系只能取：`一致`、`漂移`、`未知`。`漂移`/`未知` 必须有动作，且在写 PRD 前关闭或显式阻断。
