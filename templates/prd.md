<!--
  PRD 结构化模板 · hact-method
  规则（draft-prd-vN 结构层契约 + check-docs.js linter）：
  - 6 个必含段落用下列固定 `## ` header，顺序不限但不可缺、不可空。
  - 核心功能段每个功能用固定 header `### 功能：{名}` 切块。
  - 凡 `<待填>` 必须替换为实际内容（留着 = linter FAIL，空着即刺眼）。
  - `涉及实体` 列出的实体名，须在 TRD `### 表：{名}` 有对应表（linter 跨文件对账）。
  - 提示写在 HTML 注释里（linter 忽略）；删掉示例占位块后再交付。
-->

# PRD · v{N} · {项目名}

## 产品目标

<!-- 一句话，聚焦本期价值主张 -->
<待填>

## 目标用户

<!-- 画像 + 核心痛点 -->
<待填>

## 核心功能

### 功能：{功能名}
**入口**：<待填>
**draft-ux**：<待填>
**涉及实体**：<待填>
**场景描述**：<待填>
**Acceptance Criteria**：
- AC1：<待填>
**明确排除**：<待填>

<!-- 复制上面整块「### 功能：」追加更多功能 -->

## 用户故事

<!-- 3–5 个，覆盖核心场景 -->
- <待填>

## MVP 边界

<!-- 明确"不做什么"及原因 -->
<待填>

## 开放问题

<!-- 定稿时本段必须为空（无任何列表项）。未解决的问题在此列，全部解决后清空。 -->
