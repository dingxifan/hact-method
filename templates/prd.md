<!--
  PRD 结构化模板 · hact-method
  规则（draft-prd-vN 结构层契约 + check-docs.js linter）：
  - 6 个必含段落用下列固定 `## ` header，顺序不限但不可缺、不可空。
  - 核心功能段每个功能用固定 header `### 功能：{名}` 切块。
  - 凡 `<待填>` 必须替换为实际内容（留着 = linter FAIL，空着即刺眼）。
  - 每条 Acceptance Criteria 标全局唯一 `AC-nn`（跨功能连续编号，不按功能从 1 重号）。
    append-only：增删 AC 时 id 永不复用、允许留空号；删一条即退休其号，其余不动。
    任务包 AC 用 `(源：PRD AC-nn)` 回链此 id（check-sprint 据此机械核逐条覆盖）。
  - 每条有行为的 AC 紧跟一行行为例子 `例：Given … / When … / Then …`（测试脊柱幕 1，spec-by-example）。
    例子是 CC 起草、产品验证的「应发生什么」，自然语言、不写实现；纯视觉美学 AC（如配色）可省。
    下游：TRD 幕 2 给不可视区例子补技术精度（状态码/错误码），develop 物化成可运行测试。
    例子是散文约定（不进 linter 槽位），但「有行为的 AC 写不出例子＝坏 AC」当场暴露。
  - `涉及实体` 列出的实体名，须在 TRD `### 表：{名}` 有对应表（linter 跨文件对账）。
  - 提示写在 HTML 注释里（linter 忽略）；删掉示例占位块后再交付。
-->

# PRD · v{N} · {项目名}

<!--
  v2+ 必加「变更摘要」（v1 删除本注释块）：文件开头加
  ## vN 变更摘要
  > 供技术设计 / 开发快速定位工作范围。继承项无需重新设计，仅验证与新架构兼容。
  ### 架构层变更（有则写，无则省略）
  | # | 变更 | 影响范围 |
  |---|------|---------|
  | A1 | {变更描述} | {影响的功能或模块} |
  ### 功能变更
  | # | 功能 | 变更类型 | 说明 |
  |---|------|---------|------|
  | F1 | {功能名} | 继承 / 继承·微调 / 重构 / 简化 / 新增 | {一句话说明本期做了什么} |
  并给每个功能标题加变更类型后缀（header 仍以 `### 功能：` 起头，供 linter 切块）：
  ### 功能：功能名 `继承`
-->

## 产品目标

<!-- 一句话，聚焦本期价值主张 -->
<待填>

## 目标用户

<!-- 画像 + 核心痛点 -->
<待填>

## 核心功能

### 功能：{功能名}
**入口**：<待填>
**draft-ux**：<待填>
**涉及实体**：<待填>
**场景描述**：<待填>
**Acceptance Criteria**：
- AC-01：<待填>
  - 例：Given <前置/输入> / When <动作> / Then <期望结果>
**明确排除**：<待填>

<!-- 复制上面整块「### 功能：」追加更多功能 -->

## 用户故事

<!-- 3–5 个，覆盖核心场景 -->
- <待填>

## MVP 边界

<!-- 明确"不做什么"及原因 -->
<待填>

## 开放问题

<!-- 定稿时本段必须为空（无任何列表项）。未解决的问题在此列，全部解决后清空。 -->
