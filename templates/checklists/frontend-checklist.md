# 前端验证要点

按用户任务和本次改动画面选择验证；结果写已有任务/审查记录，不另交自检报告。

- 沿真实入口完成用户任务，检查关键判断所需信息、成功反馈和下一步；不能只证明按钮存在。
- 覆盖相关的失败/空态、取消/返回、未保存离开和恢复；跨页面保留任务上下文和输入。
- 视觉与交互以已确认 design/原型为准；实际打开运行界面，核布局、焦点、可操作性与需要的视口。
- 状态切换、异步迟到结果、重复提交和身份切换按本次契约验证；不预设所有项目相同实现。
- 类型/lint 只代表实际配置覆盖；已有未失效测试可复用，不逐项重写。不可用的浏览器能力须说明，不能用静态检查宣称体验通过。
